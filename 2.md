# 讨论模块接口文档

## 概述

本接口文档涵盖讨论区（帖子/回复）管理模块的API接口。所有HTTP API均使用统一的基础路径 `/api/v1`，并遵循统一的响应格式 `R<T>`。

### 基础路径

讨论模块基础路径：`/api/v1/discussion`

### 通用响应格式

所有HTTP接口返回统一的响应包装器 `R<T>`，结构如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| code | int | 状态码，200表示成功，其他为错误码 |
| message | string | 提示信息 |
| data | T | 实际数据，泛型类型 |

成功示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

错误示例：
```json
{
  "code": 500,
  "message": "错误描述",
  "data": null
}
```

### 数据模型说明

#### PostCreateDTO
创建帖子/回复的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ownerId | Long | 是 | 所属对象ID |
| ownerType | string | 是 | 所属对象类型：`"homework"`、`"submission"`、`"course"` 或 `"global"` |
| parentId | Long | 否 | 回复的帖子ID，若为null表示发布根帖子 |
| content | string | 是 | 帖子内容，不超过5000字符，不能为空 |
| userId | Long | 否 | 发帖用户ID（后端自动从认证信息中设置，前端无需传递） |

#### PostUpdateDTO
更新帖子内容的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| content | string | 是 | 新的帖子内容，不超过5000字符，不能为空 |

#### DiscussionPostDetail
讨论帖子详情，包含发帖人信息和嵌套的回复列表。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 帖子ID |
| ownerId | Long | 所属对象ID |
| ownerType | string | 所属对象类型：`"homework"`、`"submission"`、`"course"` 或 `"global"` |
| parentId | Long | 父帖子ID，如果为null表示是根帖子 |
| userId | Long | 发帖用户ID |
| content | string | 帖子内容，不超过5000字符 |
| isDeleted | Boolean | 是否已删除 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |
| username | string | 发帖人用户名 |
| replies | List\<DiscussionPostDetail\> | 回复列表，嵌套结构 |

### ownerType 说明

| 类型值 | 说明 | ownerId 含义 |
|--------|------|-------------|
| homework | 作业讨论区 | 作业ID |
| submission | 作业提交讨论区 | 作业提交ID |
| course | 课程讨论区 | 课程ID |
| global | 全局/班级讨论区 | 班级ID |

---

## 接口列表

### 1. 获取讨论区内容

**GET** `/api/v1/discussion/getall`

获取指定对象下的所有讨论帖子（树形结构）。

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| ownerId | Long | 是 | 所属对象ID |
| ownerType | string | 是 | 所属对象类型：`"homework"`、`"submission"`、`"course"` 或 `"global"` |

**响应**

`R<List<DiscussionPostDetail>>`，data为帖子列表（树形结构）。

**注意**：被软删除的帖子会显示为"该评论已被删除"，但树形结构仍然保留。

### 2. 发布新帖子或回复

**POST** `/api/v1/discussion/`

发布新的帖子（根帖子）或回复（回复现有帖子）。

**请求体** (`application/json`)

使用 `PostCreateDTO` 格式。

**响应**

`R<DiscussionPostDetail>`，data为新创建的帖子详情。

### 3. 修改帖子内容

**PUT** `/api/v1/discussion/{postId}`

修改指定帖子的内容。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| postId | Long | 帖子ID |

**请求体** (`application/json`)

使用 `PostUpdateDTO` 格式。

**响应**

`R<DiscussionPostDetail>`，data为更新后的帖子详情，message为"更新成功"。

**权限**：用户只能修改自己发布的帖子。

### 4. 删除帖子

**DELETE** `/api/v1/discussion/{postId}`

删除指定帖子（逻辑删除）。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| postId | Long | 帖子ID |

**响应**

`R<Void>`，成功时data为null，message为"删除成功"。

**权限**：
- 用户可以删除自己发布的帖子
- 教师/管理员可以删除其管理班级内的帖子

---

## 使用说明

1. **树形结构**：获取讨论区内容的接口返回的是树形结构，根帖子（parentId为null）的`replies`字段包含其所有回复，回复也可以嵌套回复。
2. **所属对象类型**：`ownerType` 必须是预定义的值：`"homework"`、`"submission"`、`"course"` 或 `"global"`。其他值将导致请求被拒绝。
3. **身份验证**：所有接口都需要用户认证（通过Token）。发帖人信息（userId、username）由后端自动从认证信息中获取。
4. **内容长度限制**：帖子内容不得超过5000个字符。
5. **权限控制**：
   - 读取权限：用户必须属于该对象所属的班级（或为管理员）
   - 发帖权限：同读取权限
   - 修改权限：只能修改自己的帖子
   - 删除权限：可以删除自己的帖子，教师/管理员可以删除其管理班级内的帖子

---

## 示例

### 获取作业讨论区
```
GET /api/v1/discussion/getall?ownerId=123&ownerType=homework
```

响应示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": [
    {
      "id": 1,
      "ownerId": 123,
      "ownerType": "homework",
      "parentId": null,
      "userId": 5,
      "content": "这道题怎么做？",
      "isDeleted": false,
      "createTime": "2025-01-15T10:30:00",
      "updateTime": "2025-01-15T10:30:00",
      "username": "张三",
      "replies": [
        {
          "id": 2,
          "ownerId": 123,
          "ownerType": "homework",
          "parentId": 1,
          "userId": 6,
          "content": "参考教材第5章",
          "isDeleted": false,
          "createTime": "2025-01-15T11:00:00",
          "updateTime": "2025-01-15T11:00:00",
          "username": "李老师",
          "replies": []
        }
      ]
    }
  ]
}
```

### 获取已删除帖子的讨论区
```
GET /api/v1/discussion/getall?ownerId=456&ownerType=course
```

响应示例（包含已删除帖子）：
```json
{
  "code": 200,
  "message": "Success",
  "data": [
    {
      "id": 3,
      "ownerId": 456,
      "ownerType": "course",
      "parentId": null,
      "userId": 7,
      "content": "该评论已被删除",
      "isDeleted": true,
      "createTime": "2025-01-16T09:15:00",
      "updateTime": "2025-01-16T09:15:00",
      "username": "王同学",
      "replies": []
    }
  ]
}
```

### 发布新帖子
```
POST /api/v1/discussion/
Content-Type: application/json

{
  "ownerId": 123,
  "ownerType": "homework",
  "parentId": null,
  "content": "请问截止时间是什么时候？"
}
```

响应示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    "id": 4,
    "ownerId": 123,
    "ownerType": "homework",
    "parentId": null,
    "userId": 8,
    "content": "请问截止时间是什么时候？",
    "isDeleted": false,
    "createTime": "2025-01-17T14:20:00",
    "updateTime": "2025-01-17T14:20:00",
    "username": "赵同学",
    "replies": []
  }
}
```

### 回复现有帖子
```
POST /api/v1/discussion/
Content-Type: application/json

{
  "ownerId": 123,
  "ownerType": "homework",
  "parentId": 1,
  "content": "我也有同样的问题"
}
```

### 修改帖子
```
PUT /api/v1/discussion/4
Content-Type: application/json

{
  "content": "请问提交截止时间是什么时候？"
}
```

### 删除帖子
```
DELETE /api/v1/discussion/4
```

