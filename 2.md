# 讨论模块接口文档

## 概述

本模块提供课程、作业、提交等场景下的讨论功能，支持树状结构的帖子与回复。所有接口均要求用户认证，并根据用户角色和所属班级进行权限控制。

**基础路径**：`/api/discussion`（具体前缀由 `DISCUSSION_BASE_PREFIX` 常量定义）

## 接口列表

### 1. 获取讨论树
获取指定对象（如课程、作业、提交）下的所有帖子，以树状结构返回。

**Endpoint**: `GET /getall`

**Query Parameters**:
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `ownerId` | Long | 是 | 所属对象的ID |
| `ownerType` | String | 是 | 所属对象的类型，枚举值：`homework`、`submission`、`course`、`global` |

**ownerType 说明**：
- `homework`: ownerId 为作业ID（homeworkId）
- `submission`: ownerId 为提交ID（homeSubmissionId）
- `course`: ownerId 为课程ID（courseId）
- `global`: ownerId 为班级ID（classId）

**权限要求**：
- 管理员（admin）可访问任何对象
- 学校负责人（school principal）可访问其管辖班级下的对象
- 班级成员（学生或教师）可访问自己班级下的对象
- 否则返回 `403`（无权访问）

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "ownerId": 100,
      "ownerType": "homework",
      "parentId": null,
      "userId": 5,
      "content": "这道题怎么做？",
      "isDeleted": false,
      "createTime": "2025-01-15T10:30:00",
      "updateTime": "2025-01-15T10:30:00",
      "username": "张三",
      "replies": [
        {
          "id": 2,
          "ownerId": 100,
          "ownerType": "homework",
          "parentId": 1,
          "userId": 6,
          "content": "可以参考教材第5章",
          "isDeleted": false,
          "createTime": "2025-01-15T11:00:00",
          "updateTime": "2025-01-15T11:00:00",
          "username": "李老师",
          "replies": []
        }
      ]
    }
  ]
}
```

**说明**：
- 帖子按树状结构组织，顶级帖子的 `parentId` 为 `null`
- 被软删除的帖子，其 `content` 字段会被替换为 `"该评论已被删除"`
- 每个帖子包含发帖人用户名（`username`）和嵌套的回复列表（`replies`）

---

### 2. 创建帖子/回复
发布一个新的帖子（顶级）或回复（对已有帖子的回复）。

**Endpoint**: `POST /`

**请求体**（`application/json`）：
```json
{
  "ownerId": 100,
  "ownerType": "homework",
  "parentId": null,
  "content": "这道题怎么做？"
}
```

**字段说明**：
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `ownerId` | Long | 是 |  | 所属对象的ID |
| `ownerType` | String | 是 | 枚举：`homework`、`submission`、`course`、`global` | 所属对象的类型 |
| `parentId` | Long | 否 |  | 回复的帖子ID。若为 `null` 或未提供，则表示创建顶级帖子 |
| `content` | String | 是 | 长度 ≤ 5000 | 帖子内容 |

**权限要求**：
- 用户必须对所属对象有**读取权限**（参见接口1的权限规则）
- 发帖人ID（`userId`）由后端自动从认证信息中设置，前端无需传递

**响应**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "ownerId": 100,
    "ownerType": "homework",
    "parentId": null,
    "userId": 5,
    "content": "这道题怎么做？",
    "isDeleted": false,
    "createTime": "2025-01-15T10:30:00",
    "updateTime": "2025-01-15T10:30:00",
    "username": "张三",
    "replies": []
  }
}
```

---

### 3. 更新帖子内容
修改已发布的帖子内容（仅限发帖人本人操作）。

**Endpoint**: `PUT /{postId}`

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `postId` | Long | 要更新的帖子ID |

**请求体**（`application/json`）：
```json
{
  "content": "更新后的内容"
}
```

**字段说明**：
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `content` | String | 是 | 长度 ≤ 5000 | 新的帖子内容 |

**权限要求**：
- 仅发帖人本人可修改自己的帖子
- 若帖子不存在或已被删除，返回 `404`（帖子不存在或已被删除）
- 若用户非发帖人，返回 `403`（无权修改此帖子）

**响应**：
```json
{
  "code": 200,
  "message": "更新成功",
  "data": {
    "id": 1,
    "ownerId": 100,
    "ownerType": "homework",
    "parentId": null,
    "userId": 5,
    "content": "更新后的内容",
    "isDeleted": false,
    "createTime": "2025-01-15T10:30:00",
    "updateTime": "2025-01-15T10:45:00",
    "username": "张三",
    "replies": []
  }
}
```

---

### 4. 删除帖子（软删除）
将帖子标记为已删除（软删除），帖子内容将被替换为“该评论已被删除”。

**Endpoint**: `DELETE /{postId}`

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `postId` | Long | 要删除的帖子ID |

**权限要求**：
- 发帖人本人可删除自己的帖子
- 管理员、学校负责人、班级教师可删除其管辖范围内的帖子（即对所属对象有**写入权限**）
- 若帖子不存在或已被删除，返回 `404`（帖子不存在或已被删除）
- 若无权限，返回 `403`（无权删除此帖子）

**响应**：
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

**说明**：
- 软删除后，帖子仍存在于数据库中，但 `isDeleted` 字段为 `true`
- 在获取讨论树时，被软删除的帖子内容会显示为 `"该评论已被删除"`

---

## 数据模型

### DiscussionPost（基础帖子实体）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | Long | 帖子ID，主键 |
| `ownerId` | Long | 所属对象ID |
| `ownerType` | String | 所属对象类型（`homework`、`submission`、`course`、`global`） |
| `parentId` | Long | 父帖子ID，若为 `null` 表示顶级帖子 |
| `userId` | Long | 发帖人用户ID |
| `content` | String | 帖子内容，长度 ≤ 5000 |
| `isDeleted` | Boolean | 是否已软删除 |
| `createTime` | LocalDateTime | 创建时间 |
| `updateTime` | LocalDateTime | 更新时间 |

### DiscussionPostDetail（帖子详情，用于接口返回）
继承自 `DiscussionPost`，增加以下字段：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `username` | String | 发帖人用户名 |
| `replies` | List&lt;DiscussionPostDetail&gt; | 嵌套的回复列表（树状结构） |

### PostCreateDTO（创建帖子请求）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `ownerId` | Long | 是 |  | 所属对象ID |
| `ownerType` | String | 是 | 枚举：`homework`、`submission`、`course`、`global` | 所属对象类型 |
| `parentId` | Long | 否 |  | 回复的帖子ID，若为 `null` 表示创建顶级帖子 |
| `content` | String | 是 | 长度 ≤ 5000 | 帖子内容 |
| `userId` | Long | 否 |  | 由后端自动设置，前端无需传递 |

### PostUpdateDTO（更新帖子请求）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `content` | String | 是 | 长度 ≤ 5000 | 新的帖子内容 |

---

## 权限矩阵

### 读取权限（谁可以查看帖子）
| 角色 | 条件 | 说明 |
|------|------|------|
| 管理员（admin） | 无限制 | 可查看任何对象的讨论 |
| 学校负责人（school principal） | 对象属于其管辖的班级 | 可查看该班级下所有对象的讨论 |
| 班级教师（teacher） | 对象属于其授课的班级 | 可查看该班级下所有对象的讨论 |
| 班级学生（student） | 对象属于其所在的班级 | 可查看该班级下所有对象的讨论 |

### 写入权限（谁可以创建/删除帖子）
| 操作 | 允许的角色 | 条件 |
|------|------------|------|
| 创建帖子 | 所有有读取权限的用户 | 用户必须对所属对象有读取权限 |
| 更新帖子 | 发帖人本人 | 只能修改自己的帖子 |
| 删除帖子 | 1. 发帖人本人<br>2. 管理员<br>3. 学校负责人（管辖班级）<br>4. 班级教师（授课班级） | 对所属对象有写入权限的用户可删除管辖范围内的帖子 |

**注意**：写入权限比读取权限更严格，例如班级学生可以查看讨论，但只有教师和负责人可以删除他人的帖子。

---

## 错误码

| HTTP状态码 | 错误信息 | 说明 |
|------------|----------|------|
| 400 | `ownerType 为非法值` | `ownerType` 不在枚举范围内 |
| 403 | `无权访问` | 用户对所属对象没有读取权限 |
| 403 | `无权修改此帖子` | 非发帖人尝试更新帖子 |
| 403 | `无权删除此帖子` | 用户没有删除该帖子的权限 |
| 404 | `帖子不存在或已被删除` | 指定的帖子ID不存在或已被软删除 |
| 500 | 服务器内部错误 | 系统异常 |

---

## 使用示例

### 场景：作业讨论
1. 学生进入作业详情页，前端调用 `GET /getall?ownerId=100&ownerType=homework` 获取该作业下的讨论树。
2. 学生发表提问：`POST /` 请求体 `{"ownerId":100,"ownerType":"homework","content":"这道题怎么做？"}`
3. 教师回复：`POST /` 请求体 `{"ownerId":100,"ownerType":"homework","parentId":1,"content":"可以参考教材第5章"}`
4. 学生修改自己的提问：`PUT /1` 请求体 `{"content":"这道题的具体步骤是什么？"}`
5. 教师删除不当回复：`DELETE /2`

### 树状结构展示建议
前端收到讨论树后，可递归渲染 `replies` 字段，实现嵌套的评论界面。被软删除的帖子显示为“该评论已被删除”（灰色字体）。

---

## 注意事项

1. **内容长度**：帖子内容最大长度为 5000 字符，前端可做相应校验。
2. **软删除效果**：删除后帖子内容被替换，但回复关系保持不变。
3. **时间格式**：所有时间字段为 ISO 8601 格式的字符串（如 `"2025-01-15T10:30:00"`）。
4. **枚举值**：`ownerType` 必须为 `homework`、`submission`、`course`、`global` 之一，否则返回 400。
5. **权限缓存**：前端可根据用户角色和班级信息提前判断是否有权限操作，但最终以接口返回为准。