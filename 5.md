# 组织管理模块接口文档

## 概述

本模块管理系统的**学校-班级-成员**三层组织架构及基于角色的权限控制（RBAC）。核心特性包括：

1.  **层级管理**：Admin 可管理所有学校；学校负责人（Principal）可管理本校及下属班级；班级教师（Teacher）可管理本班成员。
2.  **文件目录自动创建**：新建学校或班级时，系统自动在 `filedata` 目录下创建对应的文件夹（如 `filedata/schools/{schoolId}/classes/{classId}/`），用于存储该组织单元下的所有文件（课件、作业等）。
3.  **访客（Guest）权限**：访客是一种特殊的班级成员角色，可被授权访问班级内的**特定课程**（而非全部）。其权限通过 `course_guests` 表管理。
4.  **统一的成员管理**：提供学校成员、班级成员的添加、移除、列表查询接口。

## 数据模型

### 枚举

#### `SchoolRoleEnum`（学校角色）
| 枚举值 | 角色字符串 | 说明 |
|--------|------------|------|
| `PRINCIPAL` | `"ROLE_PRINCIPAL"` | 学校负责人，可管理本校及下属班级 |

#### `ClassesRoleEnum`（班级角色）
| 枚举值 | 角色字符串 | 说明 |
|--------|------------|------|
| `TEACHER` | `"ROLE_TEACHER"` | 班级教师，可管理本班成员（含学生、访客） |
| `STUDENT` | `"ROLE_STUDENT"` | 班级学生，可参与班级内的课程、作业等 |
| `GUEST` | `"ROLE_GUEST"` | 班级访客，仅可访问被授权的特定课程 |

### POJO（数据库实体）

#### `School`（学校）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 主键 |
| `name` | `String` | 学校名称 |
| `createTime` | `LocalDateTime` | 创建时间 |
| `updateTime` | `LocalDateTime` | 更新时间 |
| `classCount` | `Integer` | **非数据库字段**，关联的班级数量 |

#### `SchoolMember`（学校成员）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 主键 |
| `schoolId` | `Long` | 所属学校ID |
| `userId` | `Long` | 用户ID |
| `role` | `String` | 角色（固定为 `ROLE_PRINCIPAL`） |
| `joinTime` | `LocalDateTime` | 加入时间 |
| `user` | `User` | **关联对象**，用户详细信息 |
| `school` | `School` | **关联对象**，学校详细信息 |

#### `Classes`（班级）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 主键 |
| `name` | `String` | 班级名称 |
| `createTime` | `LocalDateTime` | 创建时间 |
| `updateTime` | `LocalDateTime` | 更新时间 |
| `schoolId` | `Long` | 所属学校ID |
| `members` | `List<ClassMember>` | **非数据库字段**，班级成员列表 |

#### `ClassMember`（班级成员）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 主键 |
| `classId` | `Long` | 所属班级ID |
| `userId` | `Long` | 用户ID |
| `role` | `String` | 角色（`ROLE_TEACHER`/`ROLE_STUDENT`/`ROLE_GUEST`） |
| `joinTime` | `LocalDateTime` | 加入时间 |
| `user` | `User` | **关联对象**，用户详细信息 |
| `classes` | `Classes` | **关联对象**，班级详细信息 |
| `allowedCourseIds` | `List<Long>` | **仅当 role=ROLE_GUEST 时有效**，该访客被授权访问的课程ID列表 |

#### `CourseGuest`（访客课程权限）
| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `Long` | 主键 |
| `courseId` | `Long` | 课程ID |
| `userId` | `Long` | 用户ID（访客） |
| `classId` | `Long` | 班级ID |
| `grantTime` | `LocalDateTime` | 授权时间 |

### DTO（数据传输对象）

#### 请求 DTO

**`SchoolCreateDTO` / `SchoolUpdateDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `name` | `String` | `@NotBlank`，`@Size(min=2, max=100)` | 学校名称 |

**`SchoolMemberAddDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `userNames` | `List<String>` | `@NotEmpty` | 用户名列表（原始用户名，无需前缀） |
| `role` | `String` | - | 角色，默认为 `"ROLE_PRINCIPAL"` |

**`SchoolMemberDeleteDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `userIds` | `List<Long>` | `@NotEmpty` | 待删除的用户ID列表 |

**`ClassCreateDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `name` | `String` | `@NotBlank`，`@Size(min=2, max=100)` | 班级名称 |
| `schoolId` | `Long` | - | **所属学校ID**<br>- Admin 必填<br>- 学校负责人可不传，后端自动使用其所属学校ID |

**`ClassMemberAddDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `userNames` | `List<String>` | `@NotEmpty` | 用户名列表（原始用户名，无需前缀） |
| `role` | `String` | `@NotNull` | 角色（`ROLE_TEACHER`/`ROLE_STUDENT`/`ROLE_GUEST`） |
| `allowedCourseIds` | `List<Long>` | - | **仅当 role=ROLE_GUEST 时生效**，分配给访客的课程ID列表 |

**`ClassMemberDeleteDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `userIds` | `List<Long>` | `@NotEmpty` | 待删除的用户ID列表 |

**`GuestCourseUpdateDTO`**
| 字段 | 类型 | 验证规则 | 说明 |
|------|------|----------|------|
| `courseIds` | `List<Long>` | `@NotNull` | 重新分配的课程ID列表（**全量覆盖**，传空列表代表收回所有权限） |

#### 响应 DTO

**`ClassDetailDTO`**（继承 `Classes`）
| 字段 | 类型 | 说明 |
|------|------|------|
| `memberCount` | `Integer` | 班级成员总数 |

## 接口列表

### 1. 学校管理

#### 1.1 创建学校
- **URL**: `POST /api/schools`
- **权限**: `ROLE_ADMIN`
- **请求体**: `SchoolCreateDTO`
- **响应**: `R<School>`（创建成功的学校对象）
- **业务逻辑**:
    - 创建学校记录
    - 在 `filedata/schools/{schoolId}/` 下创建学校文件夹

#### 1.2 更新学校
- **URL**: `PUT /api/schools/{schoolId}`
- **权限**: `ROLE_ADMIN`
- **请求体**: `SchoolUpdateDTO`
- **响应**: `R<School>`（更新后的学校对象）

#### 1.3 删除学校
- **URL**: `DELETE /api/schools/{schoolId}`
- **权限**: `ROLE_ADMIN`
- **业务逻辑**:
    - 检查学校下是否存在班级（若有，则禁止删除）
    - 删除学校记录
    - **不删除**对应的文件目录（保留历史文件）

#### 1.4 获取学校列表（分页）
- **URL**: `GET /api/schools`
- **权限**: `ROLE_ADMIN`（获取全部学校）、`ROLE_PRINCIPAL`（仅获取自己所在的学校）
- **查询参数**:
    - `page`（默认 1）
    - `size`（默认 10）
    - `name`（可选，学校名称模糊搜索）
- **响应**: `R<Page<School>>`（每所学校包含 `classCount` 字段）

#### 1.5 获取学校详情
- **URL**: `GET /api/schools/{schoolId}`
- **权限**: `ROLE_ADMIN` 或 `ROLE_PRINCIPAL`（且必须属于该学校）
- **响应**: `R<School>`

### 2. 学校成员管理

#### 2.1 添加学校成员（负责人）
- **URL**: `POST /api/schools/{schoolId}/members`
- **权限**: `ROLE_ADMIN`
- **请求体**: `SchoolMemberAddDTO`
- **响应**: `R<Void>`
- **业务逻辑**:
    - 将指定用户添加为学校的 `ROLE_PRINCIPAL`
    - 同一学校可有多名负责人

#### 2.2 移除学校成员
- **URL**: `DELETE /api/schools/{schoolId}/members`
- **权限**: `ROLE_ADMIN`
- **请求体**: `SchoolMemberDeleteDTO`
- **响应**: `R<Void>`

#### 2.3 获取学校成员列表
- **URL**: `GET /api/schools/{schoolId}/members`
- **权限**: `ROLE_ADMIN` 或 `ROLE_PRINCIPAL`（且必须属于该学校）
- **响应**: `R<List<SchoolMember>>`（每个成员包含 `user` 详细信息）

### 3. 班级管理

#### 3.1 创建班级
- **URL**: `POST /api/classes`
- **权限**: `ROLE_ADMIN`（可指定 `schoolId`）、`ROLE_PRINCIPAL`（自动使用其所属学校）
- **请求体**: `ClassCreateDTO`
- **响应**: `R<Classes>`（创建成功的班级对象）
- **业务逻辑**:
    - 创建班级记录
    - 在 `filedata/schools/{schoolId}/classes/{classId}/` 下创建班级文件夹

#### 3.2 更新班级
- **URL**: `PUT /api/classes/{classId}`
- **权限**: `ROLE_ADMIN` 或 `ROLE_PRINCIPAL`（且班级属于其学校）
- **请求体**: `ClassCreateDTO`（同创建）
- **响应**: `R<Classes>`

#### 3.3 删除班级
- **URL**: `DELETE /api/classes/{classId}`
- **权限**: `ROLE_ADMIN` 或 `ROLE_PRINCIPAL`（且班级属于其学校）
- **业务逻辑**:
    - 检查班级下是否存在成员（若有，则禁止删除）
    - 删除班级记录
    - **不删除**对应的文件目录

#### 3.4 获取班级列表（分页）
- **URL**: `GET /api/classes`
- **权限**:
    - `ROLE_ADMIN`：可查看所有班级（可通过 `schoolId` 过滤）
    - `ROLE_PRINCIPAL`：仅可查看其学校的班级
    - `ROLE_TEACHER` / `ROLE_STUDENT` / `ROLE_GUEST`：仅可查看自己所在的班级
- **查询参数**:
    - `page`（默认 1）
    - `size`（默认 10）
    - `schoolId`（可选，学校ID过滤）
    - `name`（可选，班级名称模糊搜索）
- **响应**: `R<Page<ClassDetailDTO>>`（包含 `memberCount`）

#### 3.5 获取班级详情
- **URL**: `GET /api/classes/{classId}`
- **权限**: `ROLE_ADMIN` 或 `ROLE_PRINCIPAL`（且班级属于其学校）或 班级成员（教师/学生/访客）
- **响应**: `R<ClassDetailDTO>`

### 4. 班级成员管理

#### 4.1 添加班级成员
- **URL**: `POST /api/classes/{classId}/members`
- **权限**: `ROLE_ADMIN`、`ROLE_PRINCIPAL`（且班级属于其学校）、`ROLE_TEACHER`（且为本班教师）
- **请求体**: `ClassMemberAddDTO`
- **响应**: `R<Void>`
- **业务逻辑**:
    - 支持批量添加
    - 若角色为 `ROLE_GUEST`，会同时向 `course_guests` 表插入 `allowedCourseIds` 的授权记录

#### 4.2 移除班级成员
- **URL**: `DELETE /api/classes/{classId}/members`
- **权限**: `ROLE_ADMIN`、`ROLE_PRINCIPAL`（且班级属于其学校）、`ROLE_TEACHER`（且为本班教师）
- **请求体**: `ClassMemberDeleteDTO`
- **响应**: `R<Void>`
- **业务逻辑**:
    - 若移除的是访客，会同步删除 `course_guests` 表中该用户的所有授权记录

#### 4.3 获取班级成员列表（按角色分组）
- **URL**: `GET /api/classes/{classId}/members`
- **权限**: `ROLE_ADMIN`、`ROLE_PRINCIPAL`（且班级属于其学校）、班级成员（教师/学生/访客）
- **查询参数**: `role`（可选，按角色过滤：`ROLE_TEACHER`/`ROLE_STUDENT`/`ROLE_GUEST`）
- **响应**: `R<List<ClassMember>>`（每个成员包含 `user` 详细信息，访客还包含 `allowedCourseIds`）

#### 4.4 更新访客课程权限
- **URL**: `PUT /api/classes/{classId}/guests/{userId}/courses`
- **权限**: `ROLE_ADMIN`、`ROLE_PRINCIPAL`（且班级属于其学校）、`ROLE_TEACHER`（且为本班教师）
- **请求体**: `GuestCourseUpdateDTO`
- **响应**: `R<Void>`
- **业务逻辑**:
    - **全量覆盖**：先删除该访客在该班级下的所有旧授权，再插入新的 `courseIds` 列表
    - 传空列表 `[]` 代表收回该访客的所有课程权限

## 权限矩阵

### 学校资源
| 操作 | Admin | 学校负责人（Principal） | 教师（Teacher） | 学生（Student） | 访客（Guest） |
|------|-------|------------------------|----------------|----------------|---------------|
| 创建学校 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 更新学校 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 删除学校 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 查看学校列表 | 全部学校 | 仅所在学校 | ❌ | ❌ | ❌ |
| 查看学校详情 | ✅ | 仅所在学校 | ❌ | ❌ | ❌ |
| 添加学校成员 | ✅ | ❌ | ❌ | ❌ | ❌ |
| 移除学校成员 | ✅ | ❌ | ❌ | ❌ | ❌ |

### 班级资源
| 操作 | Admin | 学校负责人（Principal） | 教师（Teacher） | 学生（Student） | 访客（Guest） |
|------|-------|------------------------|----------------|----------------|---------------|
| 创建班级 | ✅（可指定学校） | ✅（仅限本校） | ❌ | ❌ | ❌ |
| 更新班级 | ✅ | ✅（仅限本校班级） | ❌ | ❌ | ❌ |
| 删除班级 | ✅ | ✅（仅限本校班级） | ❌ | ❌ | ❌ |
| 查看班级列表 | 全部（可过滤） | 仅本校班级 | 仅所在班级 | 仅所在班级 | 仅所在班级 |
| 查看班级详情 | ✅ | ✅（仅限本校班级） | ✅（仅所在班级） | ✅（仅所在班级） | ✅（仅所在班级） |
| 添加班级成员 | ✅ | ✅（仅限本校班级） | ✅（仅所在班级） | ❌ | ❌ |
| 移除班级成员 | ✅ | ✅（仅限本校班级） | ✅（仅所在班级） | ❌ | ❌ |
| 更新访客课程权限 | ✅ | ✅（仅限本校班级） | ✅（仅所在班级） | ❌ | ❌ |

## 示例请求/响应

### 创建学校
**请求**
```http
POST /api/schools
Content-Type: application/json
Authorization: Bearer <admin_token>

{
  "name": "第一中学"
}
```

**响应**
```json
{
  "code": 200,
  "msg": "success",
  "data": {
    "id": 1,
    "name": "第一中学",
    "createTime": "2025-05-07T10:30:00",
    "updateTime": "2025-05-07T10:30:00",
    "classCount": 0
  }
}
```

### 添加班级成员（含访客）
**请求**
```http
POST /api/classes/5/members
Content-Type: application/json
Authorization: Bearer <teacher_token>

{
  "userNames": ["guest_user"],
  "role": "ROLE_GUEST",
  "allowedCourseIds": [101, 102]
}
```

**响应**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

### 更新访客课程权限
**请求**
```http
PUT /api/classes/5/guests/123/courses
Content-Type: application/json
Authorization: Bearer <teacher_token>

{
  "courseIds": [101, 103, 105]
}
```

**响应**
```json
{
  "code": 200,
  "msg": "success",
  "data": null
}
```

## 错误码

| 错误码 | 含义 | 可能原因 |
|--------|------|----------|
| 400 | 请求参数错误 | 验证失败、缺少必填字段 |
| 403 | 权限不足 | 非管理员尝试删除学校、非本校负责人操作班级等 |
| 404 | 资源不存在 | 学校/班级/用户不存在 |
| 409 | 资源冲突 | 学校下还有班级时尝试删除学校、班级下还有成员时尝试删除班级 |
| 500 | 服务器内部错误 | 文件目录创建失败、数据库异常等 |

## 注意事项

1.  **文件目录保留**：删除学校或班级时，对应的文件目录不会被删除，以防误删重要文件。
2.  **访客权限粒度**：访客的课程权限精确到课程ID，而非班级全部课程。
3.  **角色继承**：Admin 拥有所有权限；学校负责人拥有本校所有班级的管理权；班级教师拥有本班成员的管理权。
4.  **批量操作**：添加/移除成员均支持批量，提高效率。
5.  **全量覆盖**：更新访客课程权限时，旧权限会被完全替换，前端需注意传递完整的课程ID列表。

---
*文档生成时间：2025-05-07*