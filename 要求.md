这是一个非常聪明的思路！既然访客直接请求 `/class/{id}` 或 `/student/all` 会被后端的权限拦截报 500 错误，而请求 `/course/{id}` 是合法的，且前端的 `courseList` 已经保存了该访客有权限查看的课程，我们完全可以在前端做一个**并发聚合请求**。

也就是：当访客进入全局作业区时，前端默默地遍历 `courseList` 里所有的课程，挨个去拉取这些课程的作业，然后把它们拼接在一起展示出来。

我们需要修改两个文件来实现这个功能，并优化一下之前的空状态文案。

### 1. 核心逻辑修改：`src/pages/Homework/components/StudentDashboard.jsx`

在 `StudentDashboard` 中，我们将重写 `fetchAllHomeworks` 方法，引入针对访客的 `Promise.all` 并发请求机制。

请找到 `StudentDashboard.jsx` 中的 `fetchAllHomeworks` 函数，并替换为以下代码：

```jsx
// 加载作业列表
const fetchAllHomeworks = useCallback(async () => {
  setIsLoading(true);
  try {
    let response;

    // 【核心修复】：如果是访客，且不在单一课程上下文中，拦截并改为按课程并发获取
    if (isGuest && context !== "course") {
      if (!courseList || courseList.length === 0) {
        setRawHomeworks([]);
        setIsLoading(false);
        return;
      }

      // 仅针对访客：并发请求所有其有权限查看的课程的作业
      const fetchPromises = courseList.map((course) =>
        homeworkApi.get(`/course/${course.id}`).catch((err) => {
          console.warn(`获取课程 ${course.id} 作业失败`, err);
          return null; // 捕获单个课程的错误，防止阻断整体加载
        }),
      );

      const results = await Promise.all(fetchPromises);
      let aggregatedHws = [];

      // 遍历拼接所有结果
      results.forEach((res) => {
        if (
          res &&
          res.data &&
          res.data.code === 200 &&
          Array.isArray(res.data.data)
        ) {
          aggregatedHws.push(...res.data.data);
        }
      });

      // 按更新时间降序排列，保证最新的作业在最上面
      aggregatedHws.sort(
        (a, b) =>
          new Date(b.updateTime).getTime() - new Date(a.updateTime).getTime(),
      );
      setRawHomeworks(aggregatedHws);
      setIsLoading(false);
      return;
    }

    // 原有的非访客逻辑 或 明确的单课程逻辑
    if (context === "course" && contextId) {
      response = await homeworkApi.get(`/course/${contextId}`);
    } else if (context === "class" && contextId) {
      response = await homeworkApi.get(`/class/${contextId}`);
    } else {
      response = await homeworkApi.get("/student/all");
    }

    if (response && response.data) {
      setRawHomeworks(response.data.data || []);
    }
  } catch (error) {
    Swal.fire({ icon: "error", title: "加载作业列表失败" });
  } finally {
    setIsLoading(false);
  }
}, [context, contextId, isGuest, courseList]); // 【重要】：依赖项里必须加入 courseList
```

### 2. 交互文案修正：`src/pages/Homework/components/HomeworkList.jsx`

既然现在访客能通过并发拼接看到作业了，我们上一回合加在 `HomeworkList` 里的“访客模式下，不提供个人的全局作业大盘”这句话就不合适了。我们需要把它改回正常的提示。

找到 `HomeworkList.jsx`，将空状态判断的代码修改如下：

```jsx
// ...
const isManage = user && (isTeacher || isAdmin || isPrincipal);

// 根据不同角色，给予更人性化的空状态提示
if (!homeworks || homeworks.length === 0) {
  let emptyMessage = "当前没有作业";

  if (isManage) {
    emptyMessage = "您还没有发布任何作业";
  } else if (isGuest) {
    // 【修改】：既然现在能查出来了，如果没数据，就说明这些课程确实还没发布作业
    emptyMessage = "当前公开课程中暂未发布任何作业";
  }

  return (
    <div
      style={{ textAlign: "center", padding: "40px 20px", color: "#8c8c8c" }}
    >
      <p className={styles.placeholderText}>{emptyMessage}</p>
    </div>
  );
}
// ...
```

### 💡 现在的运行效果：

1. **解决 500 报错**：访客在 `HomeworkPage` 左侧选中了某个班级，代码不再傻傻地去请求后端严防死守的 `/api/v1/homework/class/2`。
2. **巧妙的并发聚合**：前端会拿出 `courseList` 中该访客能看到的课程，如果是课程A(17)和课程B(18)，代码会同时发出 `/course/17` 和 `/course/18`，然后将结果合并、按时间排序，完美模拟出了一份属于访客的“全局作业大盘”。
3. **前端筛选完美支持**：因为底层我们已经补全了 `extractCourseId` 安全提取器，所以聚合出来的所有作业，在页面上依然可以完美使用你要求的“全部课程”下拉框进行分类筛选！
