针对你遇到的问题，我已经精准定位到了 Bug 所在。

### 为什么会出现“按钮禁用，但回车键能发送”？

这其实是前端逻辑中的状态遗漏导致的：

1. 在文件上传的钩子 `useUploader.js` 中，当执行 `startUpload` 时，代码开启了上传状态 (`setIsUploading(true)`)，但**在上传成功后，忘记将其重置为 `false**`。这导致只要上传过一次文件（即使是很小的文件），`isUploading`状态就会永远卡在`true`。
2. 在 `AIChatWindow.jsx` 中，发送按钮的 `disabled` 属性同时判断了 `isSending` 和 `isUploading`，所以按钮永远灰态。
3. **关键所在**：键盘回车的监听事件 `handleKeyDown` 中，**只校验了 `!isSending`，并没有拦截 `isUploading**`。这就产生了这个奇妙的现象：UI按钮被判定为正在上传而禁用，但回车键绕过了这个限制成功触发了发送。

### 为什么线下没报错，只有线上出现了？

这并不是由于你的线上部署环境或脚本有问题，而是**使用场景的差异**造成的。
由于处理纯文本消息时，`startUpload` 顶部有拦截：如果判断为没有文件，会直接 `return`，**不会触发** `setIsUploading(true)`。
在线下开发测试时，你可能大多时候只是输入**纯文本**与 AI 对话，没有触发这个状态死锁；而发布到线上后，你或用户在对话中**上传了图片或文件**，触发了 `setIsUploading(true)`，进而引发了永久卡死的 Bug。

---

### 无副作用修复方案

只需修改 `src/hooks/useUploader.js` 文件，在文件上传成功的两个出口处，补上 `setIsUploading(false);` 即可。

请打开 `src/hooks/useUploader.js`，找到 `startUpload` 方法，做如下两处添加：

```javascript
const startUpload = useCallback(
  async (files) => {
    if (!files || files.length === 0) {
      return { smallFiles: [], largeFileAttachmentIds: [] };
    }

    if (!apiClient) {
      throw new Error("useUploader: apiClient is required.");
    }

    setIsUploading(true);
    setUploadProgress({});

    const smallFiles = [];
    const largeFiles = [];

    for (const file of files) {
      if (file.size <= LARGE_FILE_THRESHOLD) {
        smallFiles.push(file);
      } else {
        largeFiles.push(file);
      }
    }

    if (largeFiles.length === 0) {
      // 【修复点 1】：全是小文件时，重置上传状态
      setIsUploading(false);
      return { smallFiles, largeFileAttachmentIds: [] };
    }

    // ...中间的初始化代码保持不变...

    try {
      const largeFileAttachmentIds = await Promise.all(uploadTasks);
      // 【修复点 2】：大文件分片合并成功后，重置上传状态
      setIsUploading(false);
      return { smallFiles, largeFileAttachmentIds };
    } catch (error) {
      throw error;
    }
  },
  [apiClient, processLargeFile, updateFileProgress],
);
```

完成这两处补充后，一旦文件解析和分发完毕，上传状态就能正常释放，发送按钮也会在 AI 回复后准确恢复可点击状态。该改动限定在文件上传组件内部状态闭环，对其他模块绝对没有副作用。
