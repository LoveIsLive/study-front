为了解决校长在进入 `CourseListPage.jsx` 时页面空白以及刷新后被拦截的问题，并实现类似 `AnalysisPage.jsx` 的班级下拉筛选功能，我建议对 `src/pages/Course/CourseListPage.jsx` 进行如下核心优化。

### 修改思路：

1. **完善拦截逻辑**：在刷新页面时，`detailInfo` 可能暂时为 null。我们需要确保在用户信息加载完成后，再判定是否拦截。同时，放开对校长（Principal）和管理员（Admin）的路径限制。
2. **放开渲染守卫**：目前组件末尾的 `activeType !== "class"` 判定会导致校长和管理员在没有班级上下文时直接返回 `null`（白屏）。我们需要允许这些角色在自己的“全局视图”下进行渲染。
3. **强化校长初始化逻辑**：参照 `AnalysisPage.jsx`，在进入页面时自动为校长加载其所属学校的所有班级，并默认选中第一个班级以触发课程加载。
4. **固定下拉框宽度**：确保 UI 的整齐性。

### 具体代码修改方案：

#### 1. 修改权限拦截与渲染守卫

在 `CourseListPage.jsx` 中，找到对应的 `useEffect` 和组件末尾的守卫：

```jsx
// src/pages/Course/CourseListPage.jsx

// 1. 修改拦截器：增加 detailInfo 存在性判断，防止刷新时误拦截
useEffect(() => {
  // 只有在用户信息已加载，且确定既不是管理员也不是校长，且不在班级上下文时才拦截
  if (detailInfo && !isAdmin && !isPrincipal && activeType !== "class") {
    Swal.fire({
      icon: "warning",
      title: "提示",
      text: "只有在班级上下文中才能管理课程。",
    });
    navigate("/");
  }
}, [activeType, navigate, isAdmin, isPrincipal, detailInfo]);

// ...

// 2. 修改组件末尾的渲染守卫：允许管理员和校长在非 class 模式下渲染
if (!isAdmin && !isPrincipal && activeType !== "class") {
  return null;
}
```

#### 2. 优化校长获取班级的逻辑 (参照 AnalysisPage)

确保校长在进入页面时能正确获取班级列表：

```jsx
// 在获取班级的 useEffect 中
useEffect(() => {
  if (isAdmin && selectedSchoolId) {
    // 管理员逻辑保持不变...
    classesApi
      .get("/all", { params: { schoolId: selectedSchoolId } })
      .then((res) => {
        if (res.data?.code === 200) {
          setAdminClassList(res.data.data || []);
          if (res.data.data.length > 0)
            setSelectedAdminClassId(res.data.data[0].id);
        }
      });
  } else if (isPrincipal) {
    // 【优化】校长逻辑：获取所属学校的所有班级
    const schoolId = detailInfo?.schoolMembers?.[0]?.schoolId;
    if (schoolId) {
      classesApi.get("/all", { params: { schoolId } }).then((res) => {
        if (res.data?.code === 200) {
          const classes = res.data.data || [];
          setAdminClassList(classes);
          // 默认选中第一个班级，从而触发 loadCourses
          if (classes.length > 0) {
            setSelectedAdminClassId(classes[0].id);
          } else {
            setSelectedAdminClassId("");
          }
        }
      });
    }
  }
}, [isAdmin, isPrincipal, selectedSchoolId, detailInfo]);
```

#### 3. 固定下拉框宽度并美化 UI

在 `headerRight` 部分，为 `select` 元素设置固定宽度：

```jsx
<div className={styles.headerRight}>
  {/* 学校下拉框（管理员可见） */}
  {isAdmin && (
    <select
      className={styles.pageSizeSelect}
      style={{ marginRight: "10px", width: "160px" }} // 固定宽度
      value={selectedSchoolId}
      onChange={(e) => setSelectedSchoolId(e.target.value)}
    >
      {adminSchoolList.map((s) => (
        <option key={s.id} value={s.id}>
          {s.name}
        </option>
      ))}
    </select>
  )}

  {/* 班级下拉框（管理员和校长可见） */}
  {(isAdmin || isPrincipal) && (
    <select
      className={styles.pageSizeSelect}
      style={{ marginRight: "10px", width: "160px" }} // 固定宽度
      value={selectedAdminClassId}
      onChange={(e) => setSelectedAdminClassId(e.target.value)}
    >
      <option value="">请选择班级</option>
      {adminClassList.map((c) => (
        <option key={c.id} value={c.id}>
          {c.name}
        </option>
      ))}
    </select>
  )}

  {/* 搜索框和其他功能保持不变... */}
</div>
```

### 总结

通过以上修改：

- **校长/管理员进入权限**：通过在拦截器中加入 `detailInfo` 的判断，解决了刷新页面时的误导性弹窗问题。
- **白屏问题**：通过修改渲染守卫，允许校长在不依赖左侧树结构选中的情况下，通过页面内的下拉框进入管理模式。
- **班级联动**：校长的班级列表将按照要求调用 `/classes/all?schoolId=x` 接口，并实现自动选中和课程加载。
