这三个问题我都已经为你定位清楚，并为你提供了无副作用的精准修复方案。以下是具体的解决办法和原因分析：

### 1. 解决问题一：线上环境 WebSocket CORS 报错 (`CourseWareFlatView.jsx`)

**原因分析：**
在前端代码运行在浏览器中时，`localhost` 指向的是**用户的个人电脑**，而不是部署代码的阿里云服务器。当你在本地开发时，前端和后端都在你自己的电脑上，所以 `localhost` 能正常连接；但当你把系统部署到 `http://47.121.116.149:8080` 后，你的浏览器仍然会尝试去连接用户自己电脑上的 `localhost:8080`，这触发了浏览器的 **Private Network Access (专用网络访问)** 安全策略，从而被 CORS 拦截。

**解决办法：**
将硬编码的 `localhost` 替换为动态获取当前页面的主机地址。

请打开 `src/pages/Ware/CourseWareFlatView.jsx`，找到约第 **527** 行的 `webSocketFactory`，将其修改为如下代码：

```javascript
      // 【修改点 2】：改用 webSocketFactory，动态获取当前环境的 host
      webSocketFactory: () => {
        // 如果是本地开发环境，指向 localhost；如果是线上环境，自动使用当前部署的 origin (即 47.x.x.x:8080)
        const isLocal = window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1';
        const wsBaseUrl = isLocal ? 'http://localhost:8080' : window.location.origin;
        return new SockJS(`${wsBaseUrl}/ws/search?token=${token}`);
      },

```

---

### 2. 解决问题二：讨论区帖子权限分离 (`Post.jsx`)

**原因分析：**
在当前的 `Post.jsx` 中，编辑和删除共用了一个布尔值 `canModify`，并且该判断仅验证了“是否是本人”或“是否有 isAdmin 权限”，遗漏了对教师和校长等更高职级的判断。

**解决办法：**
将 `canModify` 拆分为 `canEdit`（仅本人可以修改）和 `canDelete`（本人或教师及以上权限可删除），并将渲染逻辑分开。

请打开 `src/pages/Homework/components/Discussion/Post.jsx`，进行如下修改：

**第一处**：修改变量提取逻辑（引入职级判断函数）。
将原有的：

```javascript
const { user } = useAuthStore();
const [isReplying, setIsReplying] = useState(false);
const [isEditing, setIsEditing] = useState(false);

const canModify = user?.name === post.username || user?.isAdmin;
```

替换为：

```javascript
const { user, isTeacher, isPrincipal } = useAuthStore();
const [isReplying, setIsReplying] = useState(false);
const [isEditing, setIsEditing] = useState(false);

// 修改权限：只能本人修改
const canEdit = user?.name === post.username;
// 删除权限：本人，或者管理员、教师、校长可以删除
const canDelete =
  canEdit ||
  user?.isAdmin ||
  (isTeacher && isTeacher()) ||
  (isPrincipal && isPrincipal());
```

**第二处**：修改按钮的渲染条件。
在下面的代码中找到原本被 `{canModify && ( ... )}` 包裹的编辑和删除按钮，将其拆开，分别用新状态控制：

```javascript
{
  /* 原来的 canModify 逻辑替换为分开的权限判断 */
}
{
  canEdit && (
    <button
      title="编辑"
      className={styles.actionButton}
      onClick={() => setIsEditing(true)}
    >
      <FontAwesomeIcon icon={faEdit} />
    </button>
  );
}
{
  canDelete && (
    <button
      title="删除"
      className={styles.actionButton}
      onClick={() => onAction("delete", { postId: post.id })}
    >
      <FontAwesomeIcon icon={faTrashAlt} />
    </button>
  );
}
```

---

### 3. 解答问题三：为什么线上报错而线下不报错？

**答案解析：**
这就是典型的 **“运行环境视角的差异”** 导致的。

1. **线下环境 (Local)**：你运行代码的电脑既充当了前端客户端（浏览器端），又充当了后端服务端。所以 JS 代码里的 `http://localhost:8080` 成功地指向了你本地运行的 Spring Boot 进程。
2. **线上环境 (Online)**：虽然你通过部署脚本把前后端都发布到了 `47.121.116.149` 上，但是**前端的 JS 代码是下载到远端用户的浏览器里执行的**。当浏览器看到代码里的 `http://localhost:8080` 时，它会去寻找**用户那台电脑的 8080 端口**。
3. **CORS 拦截**：现代浏览器有一项安全机制叫 PNA（Private Network Access）。一个公网 IP 的网页（`http://47.x.x.x`）试图访问用户的本地回环地址（`localhost` / `loopback`）时，会被浏览器判定为跨域高危行为直接掐断，所以控制台会爆出 `more-private address space loopback` 的阻止信息。

按照上述第一步使用 `window.location.origin` 动态获取前缀，即可一劳永逸地解决这个环境差异带来的问题。
