好的，根据您的要求，我们需要对 `CourseDetailPage` 的头部区域进行更细腻的排版调整：保持课程标题的动感蓝色方块设计（居中），但在其下方引入分隔线，并将“课程描述”部分改为左对齐的结构化布局。

以下是修改后的代码：

### 1. 修改 `CourseDetailPage.jsx`

在课程标题和描述之间增加分隔线以及小标题。

```jsx
// src/pages/Course/CourseDetailPage.jsx (局部修改)

return (
  <div className={styles.detailContainer}>
    <div className={styles.headerSection}>
      {/* 1. 课程名称：保持居中且带动感方块 */}
      <div className={styles.titleWrapper}>
        <h1 className={styles.title}>{courseInfo.title}</h1>
      </div>

      {/* 2. 淡灰色分隔线 */}
      <div className={styles.divider}></div>

      {/* 3. 课程描述区域：改为左对齐 */}
      <div className={styles.descriptionSection}>
        <h2 className={styles.descriptionHeader}>课程描述</h2>
        <p className={styles.descriptionText}>{courseInfo.description}</p>
      </div>
    </div>

    {/* Tabs 和内容区域保持不变 */}
    <div className={styles.tabsSection}>{/* ... */}</div>
    <div className={styles.contentSection}>{/* ... */}</div>
  </div>
);
```

### 2. 修改 `CourseDetailPage.module.css`

针对新的结构调整样式，特别是层级关系和对齐方式。

```css
/* src/pages/Course/CourseDetailPage.module.css */

.detailContainer {
  max-width: 1200px;
  margin: 0 auto;
  padding: 24px;
}

.headerSection {
  display: flex;
  flex-direction: column;
  padding: 40px 0;
  margin-bottom: 20px;
}

/* --- 课程名称方块（居中展示） --- */
.titleWrapper {
  align-self: center; /* 强制标题方块自身居中 */
  background: linear-gradient(135deg, #1890ff 0%, #0050b3 100%);
  padding: 12px 36px;
  border-radius: 12px;
  box-shadow: 0 8px 20px rgba(24, 144, 255, 0.2);
  margin-bottom: 32px;
  transition: transform 0.3s ease;
  cursor: default;
}

.titleWrapper:hover {
  transform: translateY(-3px) scale(1.02);
}

.title {
  margin: 0;
  font-size: 30px;
  color: #ffffff;
  font-weight: 700;
  text-align: center;
}

/* --- 分隔线 --- */
.divider {
  width: 100%;
  height: 1px;
  background-color: #f0f0f0; /* 淡灰色 */
  margin-bottom: 24px;
}

/* --- 课程描述区域（左对齐） --- */
.descriptionSection {
  text-align: left; /* 区域内文字左对齐 */
  padding: 0 10px;
}

.descriptionHeader {
  margin: 0 0 12px 0;
  font-size: 20px; /* 比标题小，比正文大 */
  color: #262626;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 8px;
}

/* 可选：为描述标题加一个小装饰（蓝色小竖线） */
.descriptionHeader::before {
  content: "";
  display: inline-block;
  width: 4px;
  height: 18px;
  background: #1890ff;
  border-radius: 2px;
}

.descriptionText {
  color: #595959;
  font-size: 16px;
  line-height: 1.8;
  margin: 0;
  white-space: pre-wrap; /* 保留换行 */
  word-break: break-word;
}

/* --- Tabs 区域同步调整 --- */
.tabsSection {
  display: flex;
  gap: 12px;
  border-bottom: 2px solid #f0f0f0;
  margin-bottom: 24px;
  padding-bottom: 12px;
}

/* ... 后续 TabButton 和 ContentSection 样式保持您之前的配置即可 ... */
```

### 修改要点说明：

1.  **对齐策略**：通过在 `.headerSection`（Flex 容器）中对 `.titleWrapper` 使用 `align-self: center`，实现了标题方块的独立居中；而下方的描述区域默认跟随父容器靠左。
2.  **层次感**：
    - **标题**：使用了 30px 的白字+蓝色渐变，视觉冲击力最强。
    - **描述小标题**：使用了 20px，并加了一个蓝色的小竖线伪元素（`::before`），使其既显眼又专业。
    - **正文**：16px，配合 `1.8` 的行高，阅读体验非常舒适。
3.  **分隔线**：1px 的 `#f0f0f0` 线条优雅地划分了“核心视觉区”与“信息详情区”。
4.  **换行处理**：继续保留 `white-space: pre-wrap`，确保课程描述中的手动换行能够被浏览器正确渲染。
