要实现这两个需求，核心在于区分 **“当前选中的全局课程（Selected Course）”** 和 **“当前浏览的页面课程（Active Route Course）”**。

我们需要修改全局导航组件（通常是 `MainLayout` 中的侧边栏）、全局状态管理（`authStore`）以及列表页的逻辑。

### 第一步：持久化全局选中状态 (`src/store/authStore.js`)

为了确保在新标签页中点击侧边栏时，系统仍能记住你之前点击“选择”按钮确定的课程 ID，我们需要利用 Zustand 的 `persist` 中间件将 `currentCourseId` 存入 `localStorage`。

请检查并修改你的 `src/store/authStore.js`：

```javascript
import { create } from "zustand";
import { persist } from "zustand/middleware";

const useAuthStore = create(
  persist(
    (set) => ({
      // ... 其他状态如 token, user ...
      currentCourseId: null,
      currentCourse: null,

      setCurrentCourse: (id, course) =>
        set({
          currentCourseId: id,
          currentCourse: course,
        }),

      // ... 其他方法 ...
    }),
    {
      name: "auth-storage", // 存储在 localStorage 中的键名
      partialize: (state) => ({
        currentCourseId: state.currentCourseId,
        currentCourse: state.currentCourse,
      }), // 只持久化课程相关信息
    },
  ),
);

export default useAuthStore;
```

### 第二步：修改侧边栏跳转逻辑 (`src/components/common/Layout/MainLayout.jsx`)

你需要找到侧边栏渲染“课程仓库”链接的地方。现在的逻辑可能是固定跳转到 `/` 或 `/ware/home`。我们需要将其改为根据 `currentCourseId` 动态跳转。

```jsx
// src/components/common/Layout/MainLayout.jsx
import useAuthStore from "../../../store/authStore";
import { useNavigate } from "react-router-dom";

const MainLayout = ({ children }) => {
  const navigate = useNavigate();
  const currentCourseId = useAuthStore((state) => state.currentCourseId);

  const handleWarehouseClick = () => {
    if (currentCourseId) {
      // 需求1：跳转到具体选中的课程仓库路径
      navigate(`/course/${currentCourseId}/ware`);
    } else {
      // 如果没选过课程，可以提示或跳回列表
      navigate("/course");
    }
  };

  // 在 JSX 渲染侧边栏菜单项的地方：
  return (
    <div className={styles.sidebar}>
      {/* 其他菜单... */}
      <div className={styles.menuItem} onClick={handleWarehouseClick}>
        <FontAwesomeIcon icon={faWarehouse} />
        <span>课程仓库</span>
      </div>
    </div>
  );
};
```

### 第三步：完善列表页选择逻辑 (`src/pages/Course/CourseListPage.jsx`)

确保“选择”按钮只更新全局状态，而不影响当前标签页的跳转；而点击“卡片”只负责打开新标签页。

```jsx
// src/pages/Course/CourseListPage.jsx

// 1. “选择”按钮：仅更新全局持久化状态
const handleSelectCourse = (course) => {
  setCurrentCourse(course.id, course);
  // 可以加个简单的提示
  Swal.fire({
    icon: "success",
    title: `已选中课程：${course.name}`,
    timer: 1500,
    showConfirmButton: false,
  });
};

// 2. “卡片”点击：打开新标签页浏览详情，不改变全局“选中”的课程
const handleCourseCardClick = (courseId) => {
  // 需求2：跳转的 ID 是卡片的 ID，但不会覆盖持久化的 currentCourseId
  window.open(`/course/${courseId}/ware`, "_blank");
};

// JSX 部分
<div
  className={styles.courseListItem}
  onClick={() => handleCourseCardClick(course.id)} // 点击卡片打开新页
>
  {/* ... 其他内容 ... */}
  <button
    className={styles.listSelectButton}
    onClick={(e) => {
      e.stopPropagation(); // 防止触发卡片的点击事件
      handleSelectCourse(course); // 点击“选择”更新全局 ID
    }}
    title="选择课程"
  >
    <FontAwesomeIcon icon={faCheck} /> 选择
  </button>
</div>;
```

### 总结这样做的效果：

1.  **场景 A (选择课程)**：你在列表页点击了课程 16 的“选择”按钮。此时 `localStorage` 记住了 `currentCourseId = 16`。
2.  **场景 B (打开新页)**：你点击了课程 17 的卡片。浏览器打开新标签页 `localhost:5173/course/17/ware`。
3.  **场景 C (新页内导航)**：在课程 17 的新标签页中，你点击左侧固定栏的“课程仓库”。
    - 因为 `authStore` 是持久化的，新标签页初始化时会从 `localStorage` 读取到 `currentCourseId` 是 **16**。
    - 侧边栏的 `handleWarehouseClick` 会执行 `Maps('/course/16/ware')`。
    - **结果**：路径从 `/course/17/...` 变回了 `/course/16/...`，完美符合你的需求。

**注意**：请确保你的 `App.jsx` 中已经配置了路由 `<Route path="/course/:courseId/ware/*" element={<CourseDetailPage />} />`。
