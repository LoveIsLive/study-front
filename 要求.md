通过分析你提供的代码，你的项目中**已经完美地预留了这两种模式的支持**（即 `WarePage.jsx` 中区分的 `mode="global"` 和 `mode="courseDetail"`）。

目前出现这两种操作进入相同组件（`CourseDetailPage`）的问题，根本原因在于**左侧导航栏按钮的跳转链接写错了**。

### 问题原因分析：

在你的 `src/App.jsx` 中，路由是这样定义的：

1. `<Route path="/ware/home/*" element={<WarePage />} />` (独立组件页面)
2. `<Route path="/course/:courseId/*" element={<CourseDetailPage />} />` (复合详情页面)

你目前“点击左侧导航栏”跳转的路径是 `/course/12/ware`，这被 `App.jsx` 的第2条路由捕获了，所以强行渲染了 `CourseDetailPage`。

### 解决方案

你只需要修改**左侧导航栏组件（可能是 Drawer 里的菜单或 Header 里的菜单代码，你没有发出来，但你需要去那个文件里改）**中的跳转路径即可。

#### 1. 修改左侧导航栏的跳转路径

找到你的左侧菜单项（侧边栏）代码，将“课程仓库”按钮的跳转路径进行如下修改：

**修改前：**

```javascript
// 可能是类似这样的写法
onClick={() => navigate(`/course/${activeCourseId}/ware`)}
```

**修改后：**

```javascript
// 改为指向 /ware/home 路由
onClick={() => navigate(`/ware/home/${activeCourseId}`)}
```

_(注：如果你的 `useAuthStore` 已经存了 `activeCourseId`，你甚至可以直接 `Maps('/ware/home')`，因为 `WarePage.jsx` 的 `useEffect` 里面写了兜底拦截，会自动补全为 `/ware/home/当前课程ID`)_

#### 2. 为什么这样改就能完全满足你的需求？

- **操作 A（点击左侧导航栏）：**
  - 跳转路径变成 `/ware/home/12`。
  - 被 `App.jsx` 中的 `<Route path="/ware/home/*" element={<WarePage />} />` 拦截。
  - **直接进入 `WarePage.jsx`**。
  - `WarePage.jsx` 内部的 `useMemo` 会识别 `pathParts[0] === "ware"`，将 `mode` 设为 `"global"`。
  - **左侧选中态**：因为 URL 的开头是 `/ware`，所以你的左侧导航栏可以通过匹配 `location.pathname.startsWith('/ware')` 正常高亮显示。

- **操作 B（点击课程卡片）：**
  - `CourseListPage.jsx` 里的逻辑是 `window.open("/course/" + courseId, "_blank")`。
  - 被 `App.jsx` 拦截进入 `CourseDetailPage.jsx`。
  - `CourseDetailPage.jsx` 内部的路由 `<Route path="*" element={<Navigate to="ware" replace />} />` 将路径重定向到 `/course/12/ware`。
  - 渲染的是 `CourseDetailPage` 并在其内部的 Tabs 下面渲染 `WarePage` 子组件。
  - `WarePage.jsx` 内部识别到 `pathParts[0] === "course"`，将 `mode` 设为 `"courseDetail"`，从而限制它的内部点击只在 `/course/...` 下面变化（这就是你写的“跳转隔离器”的功劳）。
  - **左侧选中态**：此时由于 URL 的前缀是 `/course`，而不是 `/ware`，左侧导航栏就不会出现“课程仓库”的选中态（符合预期）。

**总结：** 你的底层架构（`WarePage.jsx` 和 `App.jsx`）已经写得非常好了，完全支持了“独立模式”和“详情页嵌套模式”的路由隔离。你只需要**把左侧导航栏菜单项的路由从 `/course/xxx/ware` 改成 `/ware/home/xxx` 即可**。
