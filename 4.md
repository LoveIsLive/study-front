# 作业管理模块接口文档

## 概述

本接口文档涵盖作业管理模块的API接口，包括作业发布、提交、批改、附件上传下载等功能。所有HTTP API均使用统一的基础路径 `/api/v1`，并遵循统一的响应格式 `R<T>`。

### 基础路径

| 模块 | 基础路径 |
|------|----------|
| 作业管理 | `/api/v1/homework` |
| 提交管理 | `/api/v1/submission` |
| 文件上传 | `/api/v1/attach/upload` |
| 文件下载 | `/api/v1/attach/download` |

### 通用响应格式

所有HTTP接口返回统一的响应包装器 `R<T>`，结构如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| code | int | 状态码，200表示成功，其他为错误码 |
| message | string | 提示信息 |
| data | T | 实际数据，泛型类型 |

成功示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

错误示例：
```json
{
  "code": 500,
  "message": "错误描述",
  "data": null
}
```

---

## 数据模型说明

### 作业相关模型

#### HomeworkCreateDTO
创建作业的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| title | string | 是 | 作业标题，不超过255字符 |
| content | string | 否 | 作业内容描述 |
| courseId | Long | 是 | 所属课程ID |
| type | string | 否 | 作业类型，默认为"SIMPLE"（简单作业），可选"STRUCTURED"（结构化作业） |
| metaData | Map\<String, Object\> | 否 | 结构化作业的元数据（JSON对象），包含题目信息等 |
| attachmentUploadIds | List\<String\> | 否 | 大附件的上传ID列表，小附件直接通过files参数上传 |

#### HomeworkUpdateDTO
更新作业的请求体，继承自 `HomeworkCreateDTO`。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| attachmentIdsToDelete | List\<Long\> | 否 | 需要删除的已有附件ID列表 |

#### HomeworkMetaDTO
结构化作业的元数据。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| totalScore | Integer | 是 | 作业总分值，必须大于1 |
| questions | List\<QuestionItemDTO\> | 是 | 题目列表 |

#### Homework
作业实体。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 作业ID |
| teacherId | Long | 教师ID |
| classId | Long | 班级ID |
| courseId | Long | 课程ID |
| title | string | 作业标题 |
| content | string | 作业内容 |
| type | string | 作业类型："SIMPLE"或"STRUCTURED" |
| metaData | string | 元数据（JSON字符串） |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |
| attachments | List\<AttachmentDetail\> | 关联附件列表 |

#### HomeworkDetail
作业详情，继承自 `Homework`，增加扩展字段。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| teacherName | string | 教师姓名 |
| courseName | string | 课程名称 |

### 提交相关模型

#### SubmissionCreateDTO
创建作业提交的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| homeworkId | Long | 是 | 作业ID |
| content | string | 否 | 提交内容 |
| answerData | Map\<String, Object\> | 否 | 学生的答案数据（JSON对象） |
| attachmentUploadIds | List\<String\> | 否 | 大附件的上传ID列表 |

#### HomeworkSubmissionUpdateDTO
更新作业提交的请求体，继承自 `SubmissionCreateDTO`。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| attachmentIdsToDelete | List\<Long\> | 否 | 需要删除的已有附件ID列表 |

#### HomeworkSubmission
作业提交实体。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 提交ID |
| homeworkId | Long | 作业ID |
| studentId | Long | 学生ID |
| classId | Long | 班级ID |
| content | string | 提交内容 |
| status | string | 提交状态（见HomeworkSubmissionStatusEnum） |
| score | Integer | 总分 |
| answerData | string | 学生答案（JSON字符串） |
| gradingData | string | 批改结果（JSON字符串） |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |
| attachments | List\<AttachmentDetail\> | 关联附件列表 |
| homework | Homework | 关联作业信息 |

#### HomeworkSubmissionDetail
作业提交详情，继承自 `HomeworkSubmission`，增加扩展字段。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| studentName | string | 学生姓名 |

#### SubmissionGradingDTO
批改作业提交的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| submissionId | Long | 是 | 提交ID |
| generalComment | string | 否 | 总评语 |
| manualTotalScore | Integer | 否 | 手动总分 |
| details | Map\<String, QuestionGradingItem\> | 否 | 每题批改详情，key为题目ID |

#### QuestionGradingItem
题目批改项。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| score | Integer | 该题得分 |
| comment | string | 该题评语 |

### 文件上传下载模型

#### FileMetaDTO
文件元数据。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| fileName | string | 是 | 文件名，1-255字符，不能包含非法字符 |
| mimeTypeName | string | 是 | MIME类型名称 |
| fileSize | Long | 是 | 文件大小（字节） |

#### BatchUploadInitRequestDTO
批量初始化分块上传的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| files | List\<FileMetaDTO\> | 是 | 文件元数据列表 |

#### UploadInitResult
上传初始化结果。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| originalFileName | string | 原始文件名 |
| uploadId | string | 用于分块上传的ID |

#### DownloadDTO
下载令牌信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| actualPath | string | 实际文件路径 |
| fileName | string | 显示文件名 |

### 枚举类型

#### HomeworkSubmissionStatusEnum
作业提交状态枚举。

| 值 | 说明 |
|----|------|
| SUBMITTED | 已提交 |
| RETURNED | 被退回 |
| HAVE_UPDATED | 作业有更新 |
| RE_SUBMITTED | 重新提交 |
| GRADED | 已批改 |

---

## 作业管理接口（/api/v1/homework）

### 1. 教师发布作业

**POST** `/api/v1/homework/publish`

教师发布新作业。

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的HomeworkCreateDTO，需序列化为字符串 |
| files | file[] | 否 | 小附件文件列表（可直接上传的小文件） |

**注意**：`dto`参数需要以JSON字符串形式传递，例如：
```
dto={"title":"第一次作业","content":"请按时完成","courseId":123,"type":"SIMPLE"}
```

**权限**：仅当前班级的激活教师可发布作业。

**响应**

`R<HomeworkDetail>`，data为新创建的作业详情。

### 2. 修改作业

**PUT** `/api/v1/homework/{homeworkId}`

修改指定作业信息。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkId | Long | 作业ID |

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的HomeworkUpdateDTO，需序列化为字符串 |
| files | file[] | 否 | 新上传的小附件文件列表 |

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<HomeworkDetail>`，data为更新后的作业详情，message为"作业修改成功"。

### 3. 删除作业

**DELETE** `/api/v1/homework/{homeworkId}`

删除指定作业。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkId | Long | 作业ID |

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<Void>`，成功时data为null。

### 4. 查看单个作业

**GET** `/api/v1/homework/{homeworkId}`

获取指定作业的详细信息。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkId | Long | 作业ID |

**权限**：管理员、校长、教师、学生（与作业发布者一致）。

**响应**

`R<HomeworkDetail>`，data为作业详情。

### 5. 教师查看自己发布的所有作业

**GET** `/api/v1/homework/teacher/all`

获取当前教师发布的所有作业列表。

**权限**：仅教师本人。

**响应**

`R<List<HomeworkDetail>>`，data为作业列表。

### 6. 学生查看所有作业列表

**GET** `/api/v1/homework/student/all`

学生查看所在班级的所有作业列表。

**权限**：仅学生本人。

**响应**

`R<List<HomeworkDetail>>`，data为作业列表。

### 7. 查看某个班级的所有作业

**GET** `/api/v1/homework/class/{classId}`

查看指定班级的所有作业。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| classId | Long | 班级ID |

**权限**：管理员、校长、教师。

**响应**

`R<List<HomeworkDetail>>`，data为作业列表。

### 8. 查看某课程下的所有作业

**GET** `/api/v1/homework/course/{courseId}`

查看指定课程下的所有作业。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**权限**：管理员、校长、教师、学生（与作业发布者一致）。

**响应**

`R<List<HomeworkDetail>>`，data为作业列表。

### 9. 打回作业提交

**POST** `/api/v1/homework/returnSubmission/{subId}`

教师打回学生的作业提交。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| subId | Long | 提交ID |

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<HomeworkSubmissionDetail>`，data为打回后的提交详情。

---

## 提交管理接口（/api/v1/submission）

### 1. 学生提交作业

**POST** `/api/v1/submission/submit`

学生提交作业。

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的SubmissionCreateDTO，需序列化为字符串 |
| files | file[] | 否 | 小附件文件列表 |

**权限**：学生（与作业发布者一致）。

**响应**

`R<HomeworkSubmissionDetail>`，data为新创建的提交详情。

### 2. 修改作业提交

**PUT** `/api/v1/submission/{homeworkSubmissionId}`

学生修改作业提交。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkSubmissionId | Long | 提交ID |

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的HomeworkSubmissionUpdateDTO，需序列化为字符串 |
| smallFiles | file[] | 否 | 新上传的小附件文件列表 |

**权限**：仅学生本人。

**响应**

`R<HomeworkSubmissionDetail>`，data为更新后的提交详情，message为"作业提交修改成功"。

### 3. 查看作业的所有提交记录

**GET** `/api/v1/submission/{homeworkId}/submissions`

查看指定作业的所有提交记录。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkId | Long | 作业ID |

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<List<HomeworkSubmissionDetail>>`，data为提交列表。

### 4. 学生查看自己所有的提交记录

**GET** `/api/v1/submission/student/all`

学生查看自己的所有作业提交记录。

**权限**：仅学生本人。

**响应**

`R<List<HomeworkSubmissionDetail>>`，data为提交列表。

### 5. 学生查看自己某个作业的提交

**GET** `/api/v1/submission/student/{homeworkId}/submission`

学生查看自己针对某个作业的提交记录。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| homeworkId | Long | 作业ID |

**权限**：仅学生本人。

**响应**

`R<HomeworkSubmissionDetail>`，data为提交详情。

### 6. 查看单个提交

**GET** `/api/v1/submission/{submissionId}`

查看指定提交的详细信息。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| submissionId | Long | 提交ID |

**权限**：学生本人、相应教师、校长、管理员。

**响应**

`R<HomeworkSubmissionDetail>`，data为提交详情。

### 7. 教师批改作业

**POST** `/api/v1/submission/grade`

教师批改作业提交。

**请求体** (`application/json`)

使用 `SubmissionGradingDTO` 格式。

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<HomeworkSubmissionDetail>`，data为批改后的提交详情。

### 8. AI批改作业

**POST** `/api/v1/submission/{submissionId}/ai-grade`

使用AI自动批改作业提交。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| submissionId | Long | 提交ID |

**权限**：管理员、校长、教师（与作业发布者一致）。

**响应**

`R<SubmissionGradingDTO>`，data为AI生成的批改结果。

---

## 文件上传接口（/api/v1/attach/upload）

### 1. 批量初始化分块上传

**POST** `/api/v1/attach/upload/batch-init`

批量初始化大文件分块上传，获取上传ID。

**请求体** (`application/json`)

使用 `BatchUploadInitRequestDTO` 格式。

**响应**

`R<List<UploadInitResult>>`，data为每个文件的初始化结果列表。

### 2. 上传文件块

**POST** `/api/v1/attach/upload/chunk`

上传文件分块。

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| uploadId | string | 是 | 上传ID |
| chunkIndex | Integer | 是 | 分块索引（从0开始） |
| totalChunks | Integer | 是 | 总分块数 |
| chunk | file | 是 | 分块文件 |

**响应**

`R<GenericObjectResult>`，data为上传结果。

### 3. 合并文件块

**POST** `/api/v1/attach/upload/merge`

合并所有文件分块，完成文件上传。

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| uploadId | string | 是 | 上传ID |
| totalChunks | Integer | 是 | 总分块数 |

**响应**

`R<GenericObjectResult>`，data为合并结果。

---

## 文件下载接口（/api/v1/attach/download）

### 1. 生成下载令牌

**GET** `/api/v1/attach/download/get/downloadId`

生成文件下载令牌。

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件实际存储路径 |
| fileName | string | 是 | 显示文件名 |

**响应**

`R<String>`，data为下载令牌字符串，有效期为30分钟。

### 2. 下载文件

**GET** `/api/v1/attach/download/download`

使用令牌下载文件。

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件实际存储路径 |
| mode | string | 否 | 下载模式，默认为"attachment"（附件下载），可选"inline"（内联显示） |
| token | string | 是 | 下载令牌 |

**响应**

返回文件流，或错误信息。

---

## 权限说明

### 角色定义
- **管理员**：拥有所有权限
- **校长**：可管理所在学校的班级和作业
- **教师**：可管理所授班级的作业
- **学生**：只能查看和提交所在班级的作业

### 具体权限矩阵

| 操作 | 管理员 | 校长 | 教师 | 学生 |
|------|--------|------|------|------|
| 发布作业 | ✓ | ✓ | ✓（需激活教师身份） | × |
| 修改作业 | ✓ | ✓ | ✓（所属班级） | × |
| 删除作业 | ✓ | ✓ | ✓（所属班级） | × |
| 查看作业详情 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 查看班级作业列表 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 提交作业 | × | × | × | ✓（所属班级） |
| 修改提交 | × | × | × | ✓（本人提交） |
| 查看提交记录 | ✓ | ✓ | ✓（所属班级） | ✓（本人提交） |
| 批改作业 | ✓ | ✓ | ✓（所属班级） | × |
| 打回提交 | ✓ | ✓ | ✓（所属班级） | × |

---

## 使用说明

### 1. 作业发布流程

1. 教师需要先确保已切换到教师身份并激活了目标班级
2. 如果需要上传大附件，先通过文件上传接口获取上传ID
3. 构造HomeworkCreateDTO对象并序列化为JSON字符串
4. 使用`multipart/form-data`格式上传：
   ```
   POST /api/v1/homework/publish
   Content-Type: multipart/form-data
   
   dto={"title":"作业标题","content":"作业内容","courseId":123,"type":"SIMPLE","attachmentUploadIds":["uploadId1","uploadId2"]}
   files=[file1, file2]
   ```

### 2. 结构化作业

结构化作业需要传递`metaData`字段，格式为HomeworkMetaDTO：
```json
{
  "totalScore": 100,
  "questions": [
    {
      "id": "q1",
      "type": "SINGLE_CHOICE",
      "title": "问题1",
      "options": [
        {"id": "A", "content": "选项A"},
        {"id": "B", "content": "选项B"}
      ],
      "score": 10,
      "correct": "A"
    }
  ]
}
```

### 3. 文件上传下载流程

#### 大文件上传（分块上传）：
1. 调用`/batch-init`获取上传ID
2. 将文件分块，循环调用`/chunk`上传每个分块
3. 调用`/merge`合并所有分块

#### 文件下载：
1. 调用`/get/downloadId`获取下载令牌
2. 使用令牌调用`/download`下载文件

### 4. 错误处理

常见错误场景：
- **403 无权操作**：用户权限不足
- **400 参数错误**：DTO验证失败
- **404 资源不存在**：作业/提交不存在
- **500 服务器错误**：内部处理异常

---

## 示例

### 发布简单作业
```
POST /api/v1/homework/publish
Content-Type: multipart/form-data

dto={"title":"第一次作业","content":"请阅读教材第1-3章","courseId":123,"type":"SIMPLE"}
files=[attachment.pdf]
```

### 发布结构化作业
```
POST /api/v1/homework/publish
Content-Type: multipart/form-data

dto={"title":"数学测验","content":"完成以下题目","courseId":456,"type":"STRUCTURED","metaData":{"totalScore":100,"questions":[{"id":"q1","type":"SINGLE_CHOICE","title":"1+1=?","options":[{"id":"A","content":"1"},{"id":"B","content":"2"}],"score":10,"correct":"B"}]}}
```

### 学生提交作业
```
POST /api/v1/submission/submit
Content-Type: multipart/form-data

dto={"homeworkId":789,"content":"已完成作业","answerData":{"q1":"B"}}
files=[my_answer.pdf]
```

### 教师批改作业
```
POST /api/v1/submission/grade
Content-Type: application/json

{
  "submissionId": 999,
  "generalComment": "完成得很好",
  "manualTotalScore": 95,
  "details": {
    "q1": {
      "score": 10,
      "comment": "回答正确"
    }
  }
}
```

### 大文件上传流程
```
// 1. 初始化
POST /api/v1/attach/upload/batch-init
Content-Type: application/json

{
  "files": [
    {
      "fileName": "large_video.mp4",
      "mimeTypeName": "video/mp4",
      "fileSize": 104857600
    }
  ]
}

// 2. 上传分块（循环）
POST /api/v1/attach/upload/chunk
Content-Type: multipart/form-data

uploadId=xxxx-xxxx-xxxx
chunkIndex=0
totalChunks=10
chunk=[chunk0.data]

// 3. 合并
POST /api/v1/attach/upload/merge?uploadId=xxxx-xxxx-xxxx&totalChunks=10
```

### 文件下载流程
```
// 1. 获取令牌
GET /api/v1/attach/download/get/downloadId?path=attachments/abc123.pdf&fileName=作业附件.pdf

// 2. 下载文件
GET /api/v1/attach/download/download?path=attachments/abc123.pdf&token=xxxx-xxxx-xxxx
```

