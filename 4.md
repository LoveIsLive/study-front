# 作业管理模块接口文档

## 概述

本模块提供完整的作业管理功能，包括作业发布、提交、批改、附件上传下载、成绩分析等。系统支持两种作业类型：**简单作业 (SIMPLE)** 和 **结构化作业 (STRUCTURED)**。结构化作业支持题目元数据、自动批改、成绩分析等高级功能。

所有接口均要求用户认证，并根据用户角色（管理员、校长、教师、学生）和所属班级进行严格的权限控制。

## 基础路径和通用响应格式

### 基础路径
| 模块 | 基础路径 | 说明 |
|------|----------|------|
| 作业管理 | `/api/v1/homework` | 作业的增删改查、打回提交 |
| 提交管理 | `/api/v1/submission` | 作业提交、批改、查看 |
| 文件上传 | `/api/v1/attach/upload` | 大文件分块上传 |
| 文件下载 | `/api/v1/attach/download` | 文件下载（令牌验证） |
| 成绩分析 | `/api/analysis` | 课程/作业级别的统计分析 |

**注意**：具体前缀可能由常量定义（如 `HOMEWORK_BASE_PREFIX`、`SUBMISSION_BASE_PREFIX`），请以实际部署为准。

### 通用响应格式
所有接口返回统一的响应包装器 `R<T>`：
```json
{
  "code": 200,
  "message": "success",
  "data": { ... }
}
```

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `code` | int | 状态码，200表示成功，其他为错误码 |
| `message` | string | 提示信息 |
| `data` | T | 实际数据，泛型类型 |

错误示例：
```json
{
  "code": 403,
  "message": "无权访问",
  "data": null
}
```

---

## 数据模型说明

### 作业相关模型

#### HomeworkCreateDTO（创建作业请求）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `title` | String | 是 | 长度 ≤ 255 | 作业标题 |
| `content` | String | 否 |  | 作业内容描述 |
| `courseId` | Long | 是 |  | 所属课程ID |
| `type` | String | 否 | 枚举：`"SIMPLE"`、`"STRUCTURED"` | 作业类型，默认 `"SIMPLE"` |
| `metaData` | Map<String, Object> | 否 |  | 结构化作业的元数据（JSON对象），需符合 `HomeworkMetaDTO` 结构 |
| `attachmentUploadIds` | List&lt;String&gt; | 否 |  | 大附件的上传ID列表（通过文件上传接口获得） |

#### HomeworkUpdateDTO（更新作业请求）
继承自 `HomeworkCreateDTO`，增加字段：
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `attachmentIdsToDelete` | List&lt;Long&gt; | 否 | 需要删除的已有附件ID列表 |

#### Homework（作业实体）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | Long | 作业ID |
| `teacherId` | Long | 教师ID |
| `classId` | Long | 班级ID |
| `courseId` | Long | 课程ID |
| `title` | String | 作业标题 |
| `content` | String | 作业内容 |
| `type` | String | 作业类型：`"SIMPLE"` 或 `"STRUCTURED"` |
| `metaData` | String | 元数据（JSON字符串），结构化作业时存储题目信息 |
| `createTime` | LocalDateTime | 创建时间 |
| `updateTime` | LocalDateTime | 更新时间 |
| `attachments` | List&lt;AttachmentDetail&gt; | 关联附件列表（非数据库字段） |

#### HomeworkDetail（作业详情）
继承自 `Homework`，增加字段：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `teacherName` | String | 教师姓名 |
| `courseName` | String | 课程名称 |

### 提交相关模型

#### SubmissionCreateDTO（创建提交请求）
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `homeworkId` | Long | 是 | 作业ID |
| `content` | String | 否 | 提交内容（文本描述） |
| `answerData` | Map&lt;String, Object&gt; | 否 | 学生的答案数据（JSON对象），键为题目ID，值为答案 |
| `attachmentUploadIds` | List&lt;String&gt; | 否 | 大附件的上传ID列表 |

#### HomeworkSubmissionUpdateDTO（更新提交请求）
继承自 `SubmissionCreateDTO`，增加字段：
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `attachmentIdsToDelete` | List&lt;Long&gt; | 否 | 需要删除的已有附件ID列表 |

#### HomeworkSubmission（提交实体）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | Long | 提交ID |
| `homeworkId` | Long | 作业ID |
| `studentId` | Long | 学生ID |
| `classId` | Long | 班级ID |
| `content` | String | 提交内容 |
| `status` | String | 提交状态（见 `HomeworkSubmissionStatusEnum`） |
| `score` | Integer | 总分（批改后） |
| `answerData` | String | 学生答案（JSON字符串） |
| `gradingData` | String | 批改结果（JSON字符串） |
| `createTime` | LocalDateTime | 创建时间 |
| `updateTime` | LocalDateTime | 更新时间 |
| `attachments` | List&lt;AttachmentDetail&gt; | 关联附件列表（非数据库字段） |
| `homework` | Homework | 关联作业信息（非数据库字段） |

#### HomeworkSubmissionDetail（提交详情）
继承自 `HomeworkSubmission`，增加字段：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `studentName` | String | 学生姓名 |

#### SubmissionGradingDTO（批改请求）
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `submissionId` | Long | 是 | 提交ID |
| `generalComment` | String | 否 | 总评语 |
| `manualTotalScore` | Integer | 否 | 手动总分（简单作业时使用） |
| `details` | Map&lt;String, QuestionGradingItem&gt; | 否 | 每题批改详情，key为题目ID |

#### QuestionGradingItem（题目批改项）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `score` | Integer | 该题得分 |
| `comment` | String | 该题评语 |

### 结构化作业元数据模型

#### HomeworkMetaDTO（作业元数据）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `totalScore` | Integer | 是 | ≥1 | 作业总分值 |
| `questions` | List&lt;QuestionItemDTO&gt; | 是 | 至少1题 | 题目列表 |

#### QuestionItemDTO（题目项）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `id` | String | 是 |  | 题目ID，同一作业内唯一 |
| `type` | String | 是 | 枚举：`"SINGLE_CHOICE"`、`"MULTI_CHOICE"`、`"TEXT"` | 题型 |
| `title` | String | 是 |  | 题干 |
| `score` | Integer | 是 | ≥1 | 单题分值 |
| `options` | List&lt;QuestionOptionDTO&gt; | 否 |  | 选项列表（选择题时必填） |
| `correctAnswer` | Object | 否 |  | 正确答案：单选为 `String`，多选为 `List<String>`，文本题为 `String` |
| `analysis` | String | 否 |  | 解析 |
| `aiGradingCriteria` | String | 否 |  | AI评分标准（文本题时使用） |

#### QuestionOptionDTO（选项）
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `id` | String | 是 | 选项ID，同一题目内唯一 |
| `label` | String | 否 | 标签（如 "A"、"B"） |
| `text` | String | 是 | 选项内容 |

### 文件上传下载模型

#### FileMetaDTO（文件元数据）
| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| `fileName` | String | 是 | 1-255字符，不含 `<>:"/\\|?*` | 文件名 |
| `mimeTypeName` | String | 是 |  | MIME类型名称（如 `"image/png"`） |
| `fileSize` | Long | 是 | >0 | 文件大小（字节） |

#### BatchUploadInitRequestDTO（批量初始化请求）
| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `files` | List&lt;FileMetaDTO&gt; | 是 | 文件元数据列表 |

#### UploadInitResult（上传初始化结果）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `originalFileName` | String | 原始文件名 |
| `uploadId` | String | 用于分块上传的ID |
| `filePath` | String | 文件存储路径 |

#### UploadInfoRedisDTO（Redis存储的上传信息）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `filePath` | String | 文件存储路径 |
| `fileName` | String | 文件名 |
| `fileSize` | Long | 文件大小 |
| `mimeTypeName` | String | MIME类型名称 |
| `uploaderId` | String | 上传者ID |

#### DownloadDTO（下载信息）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `actualPath` | String | 实际文件路径 |
| `fileName` | String | 显示文件名 |

### 附件模型

#### Attachment（附件实体）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `id` | Long | 附件ID |
| `ownerId` | Long | 所属对象ID（作业ID或提交ID） |
| `ownerType` | String | 所属对象类型：`"homework"` 或 `"submission"` |
| `fileName` | String | 文件名 |
| `filePath` | String | 文件存储路径 |
| `fileSize` | Long | 文件大小 |
| `mimeTypeId` | Integer | MIME类型ID |
| `uploaderId` | Long | 上传者ID |
| `createTime` | LocalDateTime | 创建时间 |

#### AttachmentDetail（附件详情）
继承自 `Attachment`，增加字段：
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `mimeTypeName` | String | MIME类型名称 |

### 成绩分析模型

#### CourseAnalyticsDTO（课程分析）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `role` | String | 角色：`"STUDENT"` 或 `"TEACHER"` |
| `submissionRate` | String | 提交率（学生：个人提交率；教师：全班平均提交率） |
| `averageScore` | Integer | 平均得分 |
| `warningStudents` | Map&lt;String, String&gt; | 异动预警名单（教师端特有） |
| `diffWithClassAverage` | Integer | 与班级均分差（学生端特有） |
| `trends` | List&lt;TrendItemDTO&gt; | 历次作业趋势数据 |
| `radarData` | Map&lt;String, Integer&gt; | 能力雷达图数据（学生端） |

#### HomeworkAnalyticsDTO（作业分析）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `role` | String | 角色：`"STUDENT"` 或 `"TEACHER"` |
| `classAverage` | Integer | 班级平均分 |
| `classHighest` | Integer | 班级最高分 |
| `myScore` | Integer | 我的得分（学生端） |
| `scoreDistribution` | Map&lt;String, Integer&gt; | 成绩分布（教师端） |
| `wrongQuestions` | List&lt;WrongQuestionItemDTO&gt; | 错题集（学生：我做错的题；教师：高频错题Top 5） |

#### TrendItemDTO（趋势项）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |
| `homeworkTitle` | String | 作业标题 |
| `myScore` | Integer | 我的得分（学生视图） |
| `classAverage` | Integer | 班级平均分 |
| `highestScore` | Integer | 最高分（教师视图） |
| `submissionCount` | Integer | 提交人数（教师视图） |

#### WrongQuestionItemDTO（错题项）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| `questionId` | String | 题目ID |
| `title` | String | 题干摘要 |
| `type` | String | 题型 |
| `fullScore` | Integer | 满分 |
| `myScore` | Integer | 我的得分（学生端） |
| `aiComment` | String | AI批语（学生端） |
| `wrongCount` | Integer | 全班错误人数（教师端） |
| `errorRate` | String | 错误率（教师端） |

### 枚举类型

#### HomeworkSubmissionStatusEnum（提交状态）
| 值 | 说明 |
|----|------|
| `SUBMITTED` | 已提交 |
| `RETURNED` | 被退回（教师打回） |
| `HAVE_UPDATED` | 作业有更新（教师修改了作业内容） |
| `RE_SUBMITTED` | 重新提交（学生修改后重新提交） |
| `GRADED` | 已批改 |

---

## 作业管理接口（/api/v1/homework）

### 1. 教师发布作业
**POST** `/api/v1/homework/publish`

教师发布新作业。

**权限要求**：
- 当前身份必须是**激活的教师**（已切换到教师身份并激活目标班级）
- 教师必须属于作业所属班级

**请求格式** (`multipart/form-data`)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `dto` | String | 是 | JSON格式的 `HomeworkCreateDTO`，需序列化为字符串 |
| `files` | File[] | 否 | 小附件文件列表（可直接上传的小文件） |

**注意**：`dto`参数需要以JSON字符串形式传递，例如：
```
dto={"title":"第一次作业","content":"请按时完成","courseId":123,"type":"SIMPLE"}
```

**响应**：`R<HomeworkDetail>`，data为新创建的作业详情。

**示例**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "teacherId": 5,
    "classId": 10,
    "courseId": 123,
    "title": "第一次作业",
    "content": "请按时完成",
    "type": "SIMPLE",
    "metaData": null,
    "createTime": "2025-01-15T10:30:00",
    "updateTime": "2025-01-15T10:30:00",
    "attachments": [],
    "teacherName": "张老师",
    "courseName": "数学"
  }
}
```

### 2. 修改作业
**PUT** `/api/v1/homework/{homeworkId}`

修改指定作业信息。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**请求格式** (`multipart/form-data`)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `dto` | String | 是 | JSON格式的 `HomeworkUpdateDTO`，需序列化为字符串 |
| `files` | File[] | 否 | 新上传的小附件文件列表 |

**响应**：`R<HomeworkDetail>`，data为更新后的作业详情，message为"作业修改成功"。

### 3. 删除作业
**DELETE** `/api/v1/homework/{homeworkId}`

删除指定作业（同时删除所有关联的提交记录和附件文件）。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**响应**：`R<Void>`，成功时data为null。

### 4. 查看单个作业
**GET** `/api/v1/homework/{homeworkId}`

获取指定作业的详细信息。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 管理员、校长、教师、学生（必须属于作业所属班级）

**数据脱敏**：
- 学生查看结构化作业时，`metaData`中的`correctAnswer`、`analysis`、`aiGradingCriteria`字段会被移除

**响应**：`R<HomeworkDetail>`，data为作业详情。

### 5. 教师查看自己发布的所有作业
**GET** `/api/v1/homework/teacher/all`

获取当前教师发布的所有作业列表。

**权限要求**：
- 仅教师本人（当前激活身份为教师）

**响应**：`R<List<HomeworkDetail>>`，data为作业列表。

### 6. 学生查看所有作业列表
**GET** `/api/v1/homework/student/all`

学生查看所在班级的所有作业列表。

**权限要求**：
- 仅学生本人（当前激活身份为学生）

**响应**：`R<List<HomeworkDetail>>`，data为作业列表。

### 7. 查看某个班级的所有作业
**GET** `/api/v1/homework/class/{classId}`

查看指定班级的所有作业。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `classId` | Long | 班级ID |

**权限要求**：
- 管理员、校长、教师

**响应**：`R<List<HomeworkDetail>>`，data为作业列表。

### 8. 查看某课程下的所有作业
**GET** `/api/v1/homework/course/{courseId}`

查看指定课程下的所有作业。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `courseId` | Long | 课程ID |

**权限要求**：
- 管理员、校长、教师、学生（必须属于课程所属班级）

**响应**：`R<List<HomeworkDetail>>`，data为作业列表。

### 9. 打回作业提交
**POST** `/api/v1/homework/returnSubmission/{subId}`

教师打回学生的作业提交，学生可以重新提交。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `subId` | Long | 提交ID |

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**限制**：已批改的作业无法打回

**响应**：`R<HomeworkSubmissionDetail>`，data为打回后的提交详情。

---

## 提交管理接口（/api/v1/submission）

### 1. 学生提交作业
**POST** `/api/v1/submission/submit`

学生提交作业。

**权限要求**：
- 当前身份必须是**激活的学生**
- 学生必须属于作业所属班级
- 不能重复提交同一作业

**请求格式** (`multipart/form-data`)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `dto` | String | 是 | JSON格式的 `SubmissionCreateDTO`，需序列化为字符串 |
| `files` | File[] | 否 | 小附件文件列表 |

**响应**：`R<HomeworkSubmissionDetail>`，data为新创建的提交详情。

### 2. 修改作业提交
**PUT** `/api/v1/submission/{homeworkSubmissionId}`

学生修改作业提交（仅限被退回或有更新的作业）。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkSubmissionId` | Long | 提交ID |

**权限要求**：
- 仅学生本人
- 提交状态必须为 `RETURNED` 或 `HAVE_UPDATED`

**请求格式** (`multipart/form-data`)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `dto` | String | 是 | JSON格式的 `HomeworkSubmissionUpdateDTO`，需序列化为字符串 |
| `smallFiles` | File[] | 否 | 新上传的小附件文件列表 |

**响应**：`R<HomeworkSubmissionDetail>`，data为更新后的提交详情，message为"作业提交修改成功"。

### 3. 查看作业的所有提交记录
**GET** `/api/v1/submission/{homeworkId}/submissions`

查看指定作业的所有提交记录（教师端）。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**响应**：`R<List<HomeworkSubmissionDetail>>`，data为提交列表。

### 4. 学生查看自己所有的提交记录
**GET** `/api/v1/submission/student/all`

学生查看自己的所有作业提交记录。

**权限要求**：
- 仅学生本人（当前激活身份为学生）

**响应**：`R<List<HomeworkSubmissionDetail>>`，data为提交列表。

### 5. 学生查看自己某个作业的提交
**GET** `/api/v1/submission/student/{homeworkId}/submission`

学生查看自己针对某个作业的提交记录。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 仅学生本人

**响应**：`R<HomeworkSubmissionDetail>`，data为提交详情。

### 6. 查看单个提交
**GET** `/api/v1/submission/{submissionId}`

查看指定提交的详细信息。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `submissionId` | Long | 提交ID |

**权限要求**：
- 学生本人、相应教师、校长、管理员

**数据脱敏**：
- 学生查看他人提交时，会隐藏作业的正确答案和解析
- 未批改的提交，学生只能看到自己的提交

**响应**：`R<HomeworkSubmissionDetail>`，data为提交详情。

### 7. 教师批改作业
**POST** `/api/v1/submission/grade`

教师批改作业提交。

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**请求体** (`application/json`)：`SubmissionGradingDTO`

**批改规则**：
- 简单作业：直接设置 `manualTotalScore`（≤100）
- 结构化作业：可设置每题的 `details`，系统自动计算总分；也可直接设置 `manualTotalScore`

**响应**：`R<HomeworkSubmissionDetail>`，data为批改后的提交详情。

### 8. AI批改作业
**POST** `/api/v1/submission/{submissionId}/ai-grade`

使用AI自动批改结构化作业提交。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `submissionId` | Long | 提交ID |

**权限要求**：
- 管理员、校长、教师（与作业发布者一致）

**限制**：仅支持结构化作业

**响应**：`R<SubmissionGradingDTO>`，data为AI生成的批改结果（包含每题得分和评语）。

---

## 文件上传下载接口

### 文件上传接口（/api/v1/attach/upload）

#### 1. 批量初始化分块上传
**POST** `/api/v1/attach/upload/batch-init`

批量初始化大文件分块上传，获取上传ID。

**请求体** (`application/json`)：`BatchUploadInitRequestDTO`

**响应**：`R<List<UploadInitResult>>`，data为每个文件的初始化结果列表。

#### 2. 上传文件块
**POST** `/api/v1/attach/upload/chunk`

上传文件分块。

**请求格式** (`multipart/form-data`)
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `uploadId` | String | 是 | 上传ID |
| `chunkIndex` | Integer | 是 | 分块索引（从0开始） |
| `totalChunks` | Integer | 是 | 总分块数 |
| `chunk` | File | 是 | 分块文件 |

**响应**：`R<GenericObjectResult>`，data为上传结果。

#### 3. 合并文件块
**POST** `/api/v1/attach/upload/merge`

合并所有文件分块，完成文件上传。

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `uploadId` | String | 是 | 上传ID |
| `totalChunks` | Integer | 是 | 总分块数 |

**响应**：`R<GenericObjectResult>`，data为合并结果。

### 文件下载接口（/api/v1/attach/download）

#### 1. 生成下载令牌
**GET** `/api/v1/attach/download/get/downloadId`

生成文件下载令牌，有效期为30分钟。

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `path` | String | 是 | 文件实际存储路径 |
| `fileName` | String | 是 | 显示文件名 |

**响应**：`R<String>`，data为下载令牌字符串。

#### 2. 下载文件
**GET** `/api/v1/attach/download/download`

使用令牌下载文件。

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `path` | String | 是 | 文件实际存储路径 |
| `mode` | String | 否 | 下载模式，默认为`"attachment"`（附件下载），可选`"inline"`（内联显示） |
| `token` | String | 是 | 下载令牌 |

**响应**：返回文件流，或错误信息。

---

## 成绩分析接口（/api/analysis）

### 1. 课程宏观分析面板
**GET** `/api/analysis/course/{courseId}`

获取课程的统计分析数据，根据当前登录人角色返回不同的视图。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `courseId` | Long | 课程ID |

**权限要求**：
- 用户必须属于课程所属班级

**响应**：`R<CourseAnalyticsDTO>`

**学生视图包含**：
- 个人提交率
- 个人平均分与班级均分差
- 历次作业成绩趋势（折线图）
- 能力雷达图（基于题目类型分析）

**教师视图包含**：
- 班级平均分
- 历次作业趋势（平均分、最高分、提交人数）
- 异动预警名单（预留字段）

### 2. 单次作业微观分析面板
**GET** `/api/analysis/homework/{homeworkId}`

获取单次作业的详细分析数据，根据当前登录人角色返回不同的视图。

**路径参数**：
| 参数名 | 类型 | 说明 |
|--------|------|------|
| `homeworkId` | Long | 作业ID |

**权限要求**：
- 用户必须属于作业所属班级

**响应**：`R<HomeworkAnalyticsDTO>`

**学生视图包含**：
- 个人得分
- 班级平均分、最高分
- 个人错题集（包含AI批语）

**教师视图包含**：
- 班级平均分、最高分
- 成绩分布饼图（优秀、良好、及格、不及格）
- 高频错题Top 5（错误人数、错误率）

---

## 权限说明

### 角色定义
- **管理员 (Admin)**：拥有所有权限
- **校长 (School Principal)**：可管理所在学校的班级和作业
- **教师 (Teacher)**：可管理所授班级的作业
- **学生 (Student)**：只能查看和提交所在班级的作业

### 权限矩阵

| 操作 | 管理员 | 校长 | 教师 | 学生 |
|------|--------|------|------|------|
| 发布作业 | ✓ | ✓ | ✓（需激活教师身份） | × |
| 修改作业 | ✓ | ✓ | ✓（所属班级） | × |
| 删除作业 | ✓ | ✓ | ✓（所属班级） | × |
| 查看作业详情 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 查看班级作业列表 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 查看课程作业列表 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 提交作业 | × | × | × | ✓（所属班级） |
| 修改提交 | × | × | × | ✓（本人提交，且状态为RETURNED或HAVE_UPDATED） |
| 查看提交记录 | ✓ | ✓ | ✓（所属班级） | ✓（本人提交） |
| 批改作业 | ✓ | ✓ | ✓（所属班级） | × |
| AI批改作业 | ✓ | ✓ | ✓（所属班级） | × |
| 打回提交 | ✓ | ✓ | ✓（所属班级） | × |
| 查看课程分析 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 查看作业分析 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |

### 权限校验逻辑
1. **管理员**：直接放行所有操作
2. **校长**：检查资源所属班级是否属于其管辖的学校
3. **教师**：检查当前激活班级是否与资源所属班级一致
4. **学生**：检查当前激活班级是否与资源所属班级一致，且只能操作自己的提交

**注意**：用户必须通过身份切换功能激活相应的班级身份，才能进行对应操作。

---

## 使用说明

### 1. 作业发布流程
1. 教师需要先确保已切换到教师身份并激活了目标班级
2. 如果需要上传大附件，先通过文件上传接口获取上传ID
3. 构造 `HomeworkCreateDTO` 对象并序列化为JSON字符串
4. 使用 `multipart/form-data` 格式上传：

```
POST /api/v1/homework/publish
Content-Type: multipart/form-data

dto={"title":"作业标题","content":"作业内容","courseId":123,"type":"SIMPLE","attachmentUploadIds":["uploadId1","uploadId2"]}
files=[file1, file2]
```

### 2. 结构化作业设计
结构化作业需要传递 `metaData` 字段，格式为 `HomeworkMetaDTO`：

```json
{
  "totalScore": 100,
  "questions": [
    {
      "id": "q1",
      "type": "SINGLE_CHOICE",
      "title": "问题1",
      "options": [
        {"id": "A", "label": "A", "text": "选项A"},
        {"id": "B", "label": "B", "text": "选项B"}
      ],
      "score": 10,
      "correctAnswer": "A",
      "analysis": "正确答案是A",
      "aiGradingCriteria": "回答正确得10分，错误得0分"
    },
    {
      "id": "q2",
      "type": "TEXT",
      "title": "简述题",
      "score": 20,
      "correctAnswer": "参考答案内容",
      "aiGradingCriteria": "根据回答的完整性和准确性评分"
    }
  ]
}
```

### 3. 学生提交结构化作业
学生提交时，`answerData` 格式应与题目ID对应：

```json
{
  "homeworkId": 1,
  "content": "已完成作业",
  "answerData": {
    "q1": "A",
    "q2": "我的回答内容"
  }
}
```

### 4. 文件上传下载流程

#### 大文件上传（分块上传）：
1. 调用 `/batch-init` 获取上传ID
2. 将文件分块，循环调用 `/chunk` 上传每个分块
3. 调用 `/merge` 合并所有分块

#### 文件下载：
1. 调用 `/get/downloadId` 获取下载令牌
2. 使用令牌调用 `/download` 下载文件

### 5. 错误处理

常见错误场景：
- **403 无权操作**：用户权限不足
- **400 参数错误**：DTO验证失败
- **404 资源不存在**：作业/提交不存在
- **409 重复提交**：学生已提交过该作业
- **500 服务器错误**：内部处理异常

### 6. 数据脱敏规则
- 学生查看结构化作业时，隐藏正确答案、解析和AI评分标准
- 学生查看他人提交时，隐藏作业的敏感信息
- 未批改的提交，学生只能看到自己的提交

---

## 示例

### 发布简单作业
```
POST /api/v1/homework/publish
Content-Type: multipart/form-data

dto={"title":"第一次作业","content":"请阅读教材第1-3章","courseId":123,"type":"SIMPLE"}
files=[attachment.pdf]
```

### 发布结构化作业
```
POST /api/v1/homework/publish
Content-Type: multipart/form-data

dto={"title":"数学测验","content":"完成以下题目","courseId":456,"type":"STRUCTURED","metaData":{"totalScore":100,"questions":[{"id":"q1","type":"SINGLE_CHOICE","title":"1+1=?","options":[{"id":"A","text":"1"},{"id":"B","text":"2"}],"score":10,"correctAnswer":"B"}]}}
```

### 学生提交作业
```
POST /api/v1/submission/submit
Content-Type: multipart/form-data

dto={"homeworkId":789,"content":"已完成作业","answerData":{"q1":"B"}}
files=[my_answer.pdf]
```

### 教师批改作业
```
POST /api/v1/submission/grade
Content-Type: application/json

{
  "submissionId": 999,
  "generalComment": "完成得很好",
  "manualTotalScore": 95,
  "details": {
    "q1": {
      "score": 10,
      "comment": "回答正确"
    }
  }
}
```

### 大文件上传流程
```
// 1. 初始化
POST /api/v1/attach/upload/batch-init
Content-Type: application/json

{
  "files": [
    {
      "fileName": "large_video.mp4",
      "mimeTypeName": "video/mp4",
      "fileSize": 104857600
    }
  ]
}

// 2. 上传分块（循环）
POST /api/v1/attach/upload/chunk
Content-Type: multipart/form-data

uploadId=xxxx-xxxx-xxxx
chunkIndex=0
totalChunks=10
chunk=[chunk0.data]

// 3. 合并
POST /api/v1/attach/upload/merge?uploadId=xxxx-xxxx-xxxx&totalChunks=10
```

### 文件下载流程
```
// 1. 获取令牌
GET /api/v1/attach/download/get/downloadId?path=attachments/abc123.pdf&fileName=作业附件.pdf

// 2. 下载文件
GET /api/v1/attach/download/download?path=attachments/abc123.pdf&token=xxxx-xxxx-xxxx
```

### 课程分析查看
```
GET /api/analysis/course/123
```

### 作业分析查看
```
GET /api/analysis/homework/456
```

---

## 注意事项

1. **身份切换**：用户必须通过身份切换功能激活相应的班级身份，才能进行班级相关操作
2. **附件大小**：小附件可直接通过 `multipart/form-data` 上传，大附件需使用分块上传
3. **结构化作业**：仅结构化作业支持AI批改、成绩分析、错题集等功能
4. **数据一致性**：教师修改作业后，已有提交的状态会变为 `HAVE_UPDATED`，学生需要重新提交
5. **时间格式**：所有时间字段为 ISO 8601 格式的字符串（如 `"2025-01-15T10:30:00"`）
6. **错误重试**：文件分块上传支持断点续传，上传ID在30分钟内有效
7. **权限缓存**：前端可根据用户角色和班级信息提前判断是否有权限操作，但最终以接口返回为准