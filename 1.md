# 接口文档

## 概述

本接口文档涵盖课程管理和仓库管理模块的API接口。所有HTTP API均使用统一的基础路径 `/api/v1`，并遵循统一的响应格式 `R<T>`。

### 基础路径

+ 课程管理: `/api/v1/course`
+ 仓库管理: `/api/v1/ware/home`
+ WebSocket 搜索: `/ware/search` (消息映射)

### 通用响应格式

所有HTTP接口返回统一的响应包装器 `R<T>`，结构如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| code | int | 状态码，200表示成功，其他为错误码 |
| message | string | 提示信息 |
| data | T | 实际数据，泛型类型 |

成功示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

错误示例：
```json
{
  "code": 500,
  "message": "错误描述",
  "data": null
}
```

### 通用结果对象

大部分操作返回的结果对象都继承自 `BaseResult`：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| errorMessage | string | 错误信息，成功时为null |

具体的结果对象（如 `VoidResult`、`DirObjectResult` 等）在 `data` 字段中返回。

---

## 课程管理接口

### 1. 创建课程

**POST** `/api/v1/course/create`

**请求体** (`application/json`)

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 课程名称，不能为空 |
| description | string | 否 | 课程描述 |
| coverImage | string | 否 | 封面图片URL |

**响应**

`R<Course>`，其中 `Course` 为课程实体（字段略）。

### 2. 更新课程

**PUT** `/api/v1/course/{courseId}`

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**请求体** (`application/json`)

同创建课程的请求体。

**响应**

`R<Course>`

### 3. 删除课程

**DELETE** `/api/v1/course/{courseId}`

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**响应**

`R<Void>`，成功时 `data` 为 `null`，message 为“课程删除成功（关联资源予以保留）”。

### 4. 查询单个课程

**GET** `/api/v1/course/{courseId}`

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**响应**

`R<Course>`

### 5. 根据班级ID查询课程列表

**GET** `/api/v1/course/class/{classId}`

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| classId | Long | 班级ID |

**响应**

`R<List<Course>>`

---

## 仓库管理接口

仓库管理接口的基础路径为 `/api/v1/ware/home`。

### 1. 创建目录

**POST** `/api/v1/ware/home/create/directories`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 目录路径，必须为合法路径 |

**响应**

`R<VoidResult>`，其中 `VoidResult` 继承 `BaseResult`，无额外字段。

### 2. 上传小文件

**POST** `/api/v1/ware/home/create/files`

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| file | file | 是 | 上传的文件 |
| path | string | 是 | 文件存储路径，必须为合法路径 |
| mimeTypeName | string | 是 | 文件MIME类型名称 |

**响应**

`R<VoidResult>`

### 3. 删除目录节点

**DELETE** `/api/v1/ware/home/delete/dir`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 目录路径，必须为合法路径 |

**响应**

`R<VoidResult>`

### 4. 删除文件节点

**DELETE** `/api/v1/ware/home/delete/file`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件路径，必须为合法路径 |

**响应**

`R<VoidResult>`

### 5. 更新目录节点（重命名）

**POST** `/api/v1/ware/home/update/dir`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 原目录路径，必须为合法路径 |
| newName | string | 是 | 新目录名称 |

**响应**

`R<VoidResult>`

### 6. 更新文件节点（重命名）

**POST** `/api/v1/ware/home/update/file`

**请求体** (`application/json`)

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 原文件路径，必须为合法路径 |
| newName | string | 否 | 新文件名称（若提供） |
| file | file | 否 | 替换的文件（若提供） |
| mimeTypeName | string | 否 | 新的MIME类型名称（若提供） |

注意：`newName`、`file`、`mimeTypeName` 至少有一个不为空。

**响应**

`R<VoidResult>`

### 7. 列出目录详细内容

**GET** `/api/v1/ware/home/get/dir`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 目录路径，必须为合法路径 |

**响应**

`R<DirObjectResult>`

`DirObjectResult` 继承 `GenericObjectResult`，并包含 `fileObjectDescs` 字段，为 `FileObjectDesc` 对象数组，每个对象字段如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| name | string | 文件/目录名称 |
| type | integer | 对象类型，0为目录，1为文件 |
| createTime | LocalDateTime | 创建时间 |
| modifyTime | LocalDateTime | 修改时间 |
| size | Long | 大小（字节），目录为0 |
| mimeTypeName | string | 文件的MIME类型名称 |

### 8. 获取单个节点详细属性

**GET** `/api/v1/ware/home/get/node`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 节点路径，必须为合法路径 |

**响应**

`R<GenericObjectResult>`

`GenericObjectResult` 继承 `BaseResult`，字段如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| name | string | 文件/目录名称 |
| type | integer | 对象类型，0为目录，1为文件 |
| createTime | LocalDateTime | 创建时间 |
| modifyTime | LocalDateTime | 修改时间 |

### 9. 下载文件

**GET** `/api/v1/ware/home/download`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件路径，必须为合法路径 |
| mode | string | 否 | 下载模式，默认为 "attachment" |
| token | string | 是 | 下载令牌，需先通过 `/get/downloadId` 接口获取 |

**响应**

直接返回文件流，或错误信息。

### 10. 生成下载令牌

**GET** `/api/v1/ware/home/get/downloadId`

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件路径，必须为合法路径 |

**响应**

`R<String>`，data 为下载令牌字符串。

### 11. 初始化大文件分片上传

**POST** `/api/v1/ware/home/chunk/init`

**请求体** (`application/json`)

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件存储路径，必须为合法路径 |
| mimeTypeName | string | 是 | 文件MIME类型名称 |

**响应**

`R<InitMultiUploadResult>`

`InitMultiUploadResult` 继承 `GenericObjectResult`，并包含 `uploadId` 字段（字符串），用于后续分片上传。

### 12. 上传分片

**POST** `/api/v1/ware/home/chunk/upload`

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| chunk | file | 是 | 分片文件 |
| uploadId | string | 是 | 上传ID，从初始化接口获得 |
| chunkIndex | integer | 是 | 分片索引，从0开始 |
| totalChunks | integer | 是 | 总分片数 |

**响应**

成功返回 `R<String>`，data 为 "上传成功"，状态码 202；失败返回 `R<String>`，data 为错误信息，状态码 500。

### 13. 合并分片

**POST** `/api/v1/ware/home/chunk/merge`

**请求体** (`application/json`)

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| uploadId | string | 是 | 上传ID |
| totalChunks | integer | 是 | 总分片数 |

**响应**

成功返回 `R<String>`，data 为 "合并成功"，状态码 202；失败返回 `R<String>`，data 为错误信息，状态码 500。

### 14. 获取所有MIME类型名称

**GET** `/api/v1/ware/home/get/mime`

**响应**

`R<MimeTypeResult>`

`MimeTypeResult` 继承 `BaseResult`，包含 `mimeTypeNames` 字段（字符串列表）。

---

## WebSocket 搜索接口

### 消息映射

**消息路径** `/ware/search`

**请求体** (`application/json`)

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 搜索起始目录，必须为合法路径 |
| namePattern | string | 是 | 搜索名称模式，不能包含非法字符 |
| activeClassId | Long | 否 | 当前活跃班级ID |
| activeSchoolId | Long | 否 | 当前活跃学校ID |

**说明**

该接口通过 WebSocket 发送搜索请求，服务端通过异步方式返回搜索结果（具体返回格式未在代码中体现，可能通过其他消息通道返回）。

---

## 数据模型定义

### 请求DTO

#### CourseDTO

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 课程名称，不能为空 |
| description | string | 否 | 课程描述 |
| coverImage | string | 否 | 封面图片URL |

#### SearchRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 搜索起始目录 |
| namePattern | string | 是 | 搜索名称模式，不能包含非法字符 |
| activeClassId | Long | 否 | 当前活跃班级ID |
| activeSchoolId | Long | 否 | 当前活跃学校ID |

#### UploadFileRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件存储路径 |
| mimeTypeName | string | 是 | 文件MIME类型名称 |

#### UpdateFileRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件路径 |
| newName | string | 否 | 新文件名称 |
| file | file | 否 | 替换的文件 |
| mimeTypeName | string | 否 | 新的MIME类型名称 |

#### InitUploadBigFileRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 文件存储路径 |
| mimeTypeName | string | 是 | 文件MIME类型名称 |

#### UploadChunkRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| uploadId | string | 是 | 上传ID |
| chunkIndex | integer | 是 | 分片索引，从0开始 |
| totalChunks | integer | 是 | 总分片数 |

#### MergeChunkRequestDTO

继承 `BaseRequestDTO`，包含以下字段：

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| uploadId | string | 是 | 上传ID |
| totalChunks | integer | 是 | 总分片数 |

### 结果对象

#### BaseResult

| 字段名 | 类型 | 说明 |
|--------|------|------|
| success | boolean | 操作是否成功 |
| errorMessage | string | 错误信息 |

#### VoidResult

继承 `BaseResult`，无额外字段。用于仅需成功/失败状态的场景。

#### GenericObjectResult

继承 `BaseResult`，包含以下字段：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| name | string | 文件/目录名称 |
| type | integer | 对象类型，0为目录，1为文件 |
| createTime | LocalDateTime | 创建时间 |
| modifyTime | LocalDateTime | 修改时间 |

#### DirObjectResult

继承 `GenericObjectResult`，增加 `fileObjectDescs` 字段，为 `FileObjectDesc` 对象数组（结构见上文）。

#### InitMultiUploadResult

继承 `GenericObjectResult`，增加 `uploadId` 字段（字符串）。

#### MimeTypeResult

继承 `BaseResult`，增加 `mimeTypeNames` 字段（字符串列表）。

---

## 注意事项

1. 所有路径参数和查询参数中的路径必须符合系统定义的合法路径规则（通过 `PathUtils` 校验）。
2. 文件上传接口使用 `multipart/form-data` 格式，注意字段名。
3. 下载文件需要先获取下载令牌（`/get/downloadId`），并在下载接口中提供该令牌。
4. 大文件上传采用分片上传机制，需先初始化，然后依次上传分片，最后合并。
5. 搜索功能通过 WebSocket 实现，前端需建立 WebSocket 连接并发送消息到指定路径。

