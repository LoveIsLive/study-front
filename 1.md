# 课程管理模块接口文档

## 概述

本接口文档涵盖课程管理模块的API接口。所有HTTP API均使用统一的基础路径 `/api/v1`，并遵循统一的响应格式 `R<T>`。

### 基础路径

课程管理基础路径：`/api/v1/course`

### 通用响应格式

所有HTTP接口返回统一的响应包装器 `R<T>`，结构如下：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| code | int | 状态码，200表示成功，其他为错误码 |
| message | string | 提示信息 |
| data | T | 实际数据，泛型类型 |

成功示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": { ... }
}
```

错误示例：
```json
{
  "code": 500,
  "message": "错误描述",
  "data": null
}
```

### 数据模型说明

#### CourseDTO
创建/更新课程的请求体。

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| name | string | 是 | 课程名称，不能为空 |
| description | string | 否 | 课程描述 |
| coverImage | string | 否 | 封面图片路径（后端自动生成，前端无需传递） |

#### Course
课程实体，返回给前端的完整课程信息。

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | Long | 课程ID |
| classId | Long | 所属班级ID |
| teacherId | Long | 教师ID |
| name | string | 课程名称 |
| description | string | 课程描述 |
| coverImage | string | 封面图片存储路径 |
| createTime | LocalDateTime | 创建时间 |
| updateTime | LocalDateTime | 更新时间 |
| teacherName | string | 教师姓名（连表查询字段，用于前端展示） |

---

## 接口列表

### 1. 创建课程

**POST** `/api/v1/course/create`

创建新课程，支持上传封面图片。

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的CourseDTO，需序列化为字符串 |
| coverImage | file | 否 | 封面图片文件 |

**注意**：`dto`参数需要以JSON字符串形式传递，例如：
```
dto={"name":"数学","description":"高等数学课程"}
```

**权限**：仅当前班级的激活教师可创建课程。

**响应**

`R<Course>`，data为新创建的课程实体。

**业务逻辑**：
- 创建课程记录
- 自动生成封面图片存储路径并保存文件
- 在虚拟文件系统中创建课程隔离目录：`/ware/{schoolId}/{classId}/{courseId}`

### 2. 更新课程

**PUT** `/api/v1/course/{courseId}`

更新指定课程信息，支持更新封面图片。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**请求格式** (`multipart/form-data`)

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| dto | string | 是 | JSON格式的CourseDTO，需序列化为字符串 |
| coverImage | file | 否 | 新的封面图片文件（如果提供则替换原封面） |

**权限**：仅课程所属班级的教师或管理员可更新。

**响应**

`R<Course>`，data为更新后的课程实体。

**业务逻辑**：
- 更新课程基本信息
- 如果提供了新封面图片，则上传新文件并删除原封面文件

### 3. 删除课程

**DELETE** `/api/v1/course/{courseId}`

删除指定课程（遵守不级联删除原则）。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**权限**：仅课程所属班级的教师或管理员可删除。

**响应**

`R<Void>`，成功时data为null，message为"课程删除成功（关联资源予以保留）"。

**注意**：
- 仅删除课程主表数据
- 保留底层`/ware`下的文件系统数据
- 保留相关的作业试卷记录

### 4. 查询单个课程

**GET** `/api/v1/course/{courseId}`

获取指定课程的详细信息。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| courseId | Long | 课程ID |

**权限**：当前用户必须属于该课程所属班级（或为管理员）。

**响应**

`R<Course>`，data为课程实体。

### 5. 根据班级ID查询课程列表

**GET** `/api/v1/course/class/{classId}`

获取指定班级下的所有课程列表。

**路径参数**

| 参数名 | 类型 | 说明 |
|--------|------|------|
| classId | Long | 班级ID |

**权限**：当前用户必须属于该班级（或为管理员）。

**响应**

`R<List<Course>>`，data为课程列表。

### 6. 获取封面图片

**GET** `/api/v1/course/getCoverImage`

获取封面图片文件流。

**查询参数**

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| path | string | 是 | 封面图片存储路径 |

**响应**

返回图片文件流，包含以下Headers：
- `Content-Type`: 根据文件MIME类型自动识别
- `Content-Length`: 文件大小
- `Cache-Control`: `max-age=31536000, public`（缓存1年）

如果图片不存在，返回404。

---

## 权限说明

### 角色定义
- **管理员**：拥有所有权限
- **校长**：可管理所在学校的班级和课程
- **教师**：可管理所授班级的课程
- **学生**：只能查看所在班级的课程

### 具体权限

| 操作 | 管理员 | 校长 | 教师 | 学生 |
|------|--------|------|------|------|
| 创建课程 | ✓ | ✓ | ✓（需激活教师身份） | × |
| 更新课程 | ✓ | ✓ | ✓（所属班级） | × |
| 删除课程 | ✓ | ✓ | ✓（所属班级） | × |
| 查看课程详情 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |
| 查看班级课程列表 | ✓ | ✓ | ✓（所属班级） | ✓（所属班级） |

**重要**：创建课程时，用户必须已切换到教师身份并激活了对应的班级成员身份。

---

## 使用说明

### 1. 创建课程流程

1. 前端需要先确保用户已切换到教师身份并激活了目标班级
2. 构造CourseDTO对象并序列化为JSON字符串
3. 使用`multipart/form-data`格式上传：
   ```
   POST /api/v1/course/create
   Content-Type: multipart/form-data
   
   dto={"name":"课程名称","description":"课程描述"}
   coverImage=[文件]
   ```

### 2. 封面图片处理

- 封面图片存储路径由后端自动生成，格式为：`coverimage/{uuid}.{extension}`
- 前端可通过`Course`对象中的`coverImage`字段获取路径
- 使用`/getCoverImage`接口获取图片文件流：
  ```
  GET /api/v1/course/getCoverImage?path=coverimage/xxxxxx.jpg
  ```

### 3. 错误处理

常见错误场景：
- **403 无权操作**：用户权限不足
- **400 参数错误**：CourseDTO验证失败
- **404 资源不存在**：课程不存在或封面图片不存在
- **500 服务器错误**：内部处理异常

---

## 示例

### 创建课程
```
POST /api/v1/course/create
Content-Type: multipart/form-data

dto={"name":"高等数学","description":"大学一年级高等数学课程"}
coverImage=[math.jpg]
```

响应示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": {
    "id": 123,
    "classId": 5,
    "teacherId": 10,
    "name": "高等数学",
    "description": "大学一年级高等数学课程",
    "coverImage": "coverimage/abc123.jpg",
    "createTime": "2025-01-15T10:30:00",
    "updateTime": "2025-01-15T10:30:00",
    "teacherName": "张老师"
  }
}
```

### 获取封面图片
```
GET /api/v1/course/getCoverImage?path=coverimage/abc123.jpg
```

响应：图片文件流，可直接在`<img>`标签中使用：
```html
<img src="/api/v1/course/getCoverImage?path=coverimage/abc123.jpg" alt="课程封面" />
```

### 查询班级课程列表
```
GET /api/v1/course/class/5
```

响应示例：
```json
{
  "code": 200,
  "message": "Success",
  "data": [
    {
      "id": 123,
      "classId": 5,
      "teacherId": 10,
      "name": "高等数学",
      "description": "大学一年级高等数学课程",
      "coverImage": "coverimage/abc123.jpg",
      "createTime": "2025-01-15T10:30:00",
      "updateTime": "2025-01-15T10:30:00",
      "teacherName": "张老师"
    },
    {
      "id": 124,
      "classId": 5,
      "teacherId": 11,
      "name": "大学英语",
      "description": "大学英语四级课程",
      "coverImage": "coverimage/def456.jpg",
      "createTime": "2025-01-16T09:15:00",
      "updateTime": "2025-01-16T09:15:00",
      "teacherName": "王老师"
    }
  ]
}
```

### 更新课程（不修改封面）
```
PUT /api/v1/course/123
Content-Type: multipart/form-data

dto={"name":"高等数学（修订版）","description":"更新后的课程描述"}
```

### 更新课程（修改封面）
```
PUT /api/v1/course/123
Content-Type: multipart/form-data

dto={"name":"高等数学（修订版）","description":"更新后的课程描述"}
coverImage=[new_cover.jpg]
```

